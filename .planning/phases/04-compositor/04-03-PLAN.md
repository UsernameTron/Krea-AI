---
phase: 04-compositor
plan: 03
type: execute
wave: 1
depends_on: [04-01, 04-02]
files_modified:
  - mirror-post/src/compositor/constants.js
  - mirror-post/src/compositor/render-chrome.js
  - mirror-post/src/compositor/assets/linkedin-logo.svg
  - mirror-post/src/compositor/fonts/NotoColorEmoji.ttf
  - mirror-post/src/brief/schema.js
  - mirror-post/src/brief/structured-outputs-schema.js
  - mirror-post/src/brief/prompts/system-prompt-builder.js
  - mirror-post/src/brief/prompts/persona-voice-distillate.md
  - mirror-post/test/fixtures/expected-briefs/brent-vellum.json
  - mirror-post/test/compositor-chrome.js
  - mirror-post/test/fixtures/golden-outputs/brent-vellum-golden.png
autonomous: false
gap_closure: true
requirements: [REQ-M-040, REQ-M-044, REQ-X-052, REQ-X-064]
must_haves:
  truths:
    - "Chrome top nav and profile bar render in real LinkedIn blue #0A66C2, not orange #0b5ca8"
    - "LinkedIn 'in' logo renders top-left of nav bar"
    - "Reaction emoji glyphs render visibly (not just colored circles)"
    - "Avatar background matches chrome LinkedIn blue #0A66C2"
    - "nav_easter_eggs field is gone from schema, fixtures, prompts, compositor, tests"
    - "Nav labels are hardcoded: Home, My Network, Jobs, Messaging, Notifications"
    - "Golden PNG regenerated and byte-match test passes"
    - "npm run test:all exits 0 with 187+ tests, 0 failing, 0 skipped"
  artifacts:
    - path: "mirror-post/src/compositor/constants.js"
      provides: "LinkedIn blue palette"
      contains: "#0A66C2"
    - path: "mirror-post/src/compositor/assets/linkedin-logo.svg"
      provides: "LinkedIn 'in' mark, white on transparent"
    - path: "mirror-post/src/compositor/fonts/NotoColorEmoji.ttf"
      provides: "Color emoji glyph coverage for reaction bar"
    - path: "mirror-post/test/fixtures/golden-outputs/brent-vellum-golden.png"
      provides: "Regenerated determinism anchor reflecting new palette + logo + emoji"
  key_links:
    - from: "render-chrome.js"
      to: "NotoColorEmoji.ttf"
      via: "registerFont + fillText font stack"
      pattern: "Noto Color Emoji"
    - from: "render-chrome.js"
      to: "linkedin-logo.svg"
      via: "loadImage at nav top-left"
      pattern: "linkedin-logo"
    - from: "constants.js"
      to: "render-chrome.js"
      via: "CHROME_BLUE and COLORS.avatarBg"
      pattern: "#0A66C2"
---

<objective>
Close Phase 4 visual UAT gaps: fix chrome palette, add LinkedIn logo, render reaction emoji glyphs, match avatar to chrome color, and eliminate the `nav_easter_eggs` field cross-phase. Regenerate golden PNG and pause at a human UAT gate. Do not close Phase 4 in this plan.

Purpose: Phase 4 verification passed automated checks but visual inspection revealed wrong chrome color (orange instead of LinkedIn blue), missing LinkedIn logo, invisible reaction emoji glyphs (colored circles only), and a stale satirical `nav_easter_eggs` field that produced misspelled nav labels. These are blockers for product-level acceptance.

Output:
- Corrected chrome renderer (LinkedIn blue #0A66C2, logo, emoji glyphs, blue avatar)
- Schema / fixtures / prompt / test surface area free of `nav_easter_eggs`
- Regenerated and committed `brent-vellum-golden.png`
- Green regression suite
- Human UAT checkpoint ready for operator review
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compositor/04-VERIFICATION.md
@.planning/phases/04-compositor/04-HUMAN-UAT.md
@mirror-post/src/compositor/constants.js
@mirror-post/src/compositor/render-chrome.js
@mirror-post/src/brief/schema.js
@mirror-post/src/brief/structured-outputs-schema.js
@mirror-post/src/brief/prompts/system-prompt-builder.js
@mirror-post/scripts/generate-golden.mjs

<interfaces>
Current constants.js (to be changed):
- `CHROME_BLUE = '#0b5ca8'`  -> must become `'#0A66C2'`
- `COLORS.chromeBg = '#0b5ca8'` -> `'#0A66C2'`
- `COLORS.avatarBg = '#0b5ca8'` -> `'#0A66C2'`
- `COLORS.tweetHandleBlue = '#1D9BF0'` (already correct, leave)
- `COLORS.verifyBadgeBlue = '#1D9BF0'` (already correct, leave)

Current render-chrome.js contracts:
- `registerFont(interRegular, { family: 'Inter', weight: 'normal' })` at lines 20-22
- `NAV_LABELS = ['Home', 'Network', 'Jobs', 'Messaging', 'Notifications']` — change 'Network' to 'My Network'
- Lines 83-85: `const navLabels = brief.nav_easter_eggs?.length === 5 ? brief.nav_easter_eggs : NAV_LABELS` — delete conditional, use NAV_LABELS directly
- Lines 220-245 (reaction draw): colored circle via `ctx.arc/fill`, then `ctx.fillText(reactionDef.emoji, cx, cy)` with `ctx.font = '16px Inter'` — change font to `'16px "Noto Color Emoji"'`

Schema contract:
- `SCHEMA.optional_keys: ["nav_easter_eggs"]` -> `[]`
- Remove validation branch at lines 52-55
- `OPTIONAL_PARAM_COUNT = 1` -> `0` (DECISION_GATE <= 24 still PASS)

Bug 3 root cause (diagnosed from render-chrome.js read):
Inter font is the only registered face; it has no color emoji glyphs. Cairo's font fallback in node-canvas does not reach system color emoji fonts when only Inter is registered. The colored background circle draws (via ctx.arc/fill), but `fillText(emoji, ...)` produces no visible glyph. Fix: bundle `NotoColorEmoji.ttf` into `src/compositor/fonts/`, register as family `"Noto Color Emoji"`, set `ctx.font = '16px "Noto Color Emoji"'` for the emoji fillText call only (keep Inter for labels and counts).
</interfaces>
</context>

<tasks>

<!-- ============================================================ -->
<!-- WAVE 1: Visual bug fixes (parallel-safe, all in compositor)  -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 1: Fix chrome palette to LinkedIn blue #0A66C2 (Bug 1 + Bug 4)</name>
  <files>mirror-post/src/compositor/constants.js</files>
  <read_first>mirror-post/src/compositor/constants.js</read_first>
  <action>
    Replace chrome palette values in constants.js:
    - `CHROME_BLUE = '#0b5ca8'` -> `CHROME_BLUE = '#0A66C2'`
    - `COLORS.chromeBg: '#0b5ca8'` -> `'#0A66C2'`
    - `COLORS.avatarBg: '#0b5ca8'` -> `'#0A66C2'` (Bug 4: avatar matches chrome)
    Do NOT touch `tweetHandleBlue` or `verifyBadgeBlue` — both already `#1D9BF0` (correct).
    Do NOT touch any other color. No other file changes in this task.
    Addresses REQ-M-040, REQ-X-064. Gap reason: visual UAT showed orange chrome.
  </action>
  <acceptance_criteria>
    - `grep -c "0A66C2" mirror-post/src/compositor/constants.js` returns >= 3
    - `grep -c "0b5ca8" mirror-post/src/compositor/constants.js` returns 0
    - `grep "1D9BF0" mirror-post/src/compositor/constants.js` still matches (unchanged)
  </acceptance_criteria>
  <verify>
    <automated>cd mirror-post && node -e "import('./src/compositor/constants.js').then(m => { if (m.CHROME_BLUE !== '#0A66C2') process.exit(1); if (m.COLORS.avatarBg !== '#0A66C2') process.exit(1); })"</automated>
  </verify>
  <done>CHROME_BLUE, chromeBg, avatarBg all equal `#0A66C2`; verify badge / handle blue untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add LinkedIn "in" logo to nav bar (Bug 2)</name>
  <files>
    mirror-post/src/compositor/assets/linkedin-logo.svg
    mirror-post/src/compositor/render-chrome.js
  </files>
  <read_first>
    mirror-post/src/compositor/render-chrome.js
    mirror-post/src/compositor/assets/
  </read_first>
  <action>
    1. Create `src/compositor/assets/linkedin-logo.svg`: white "in" mark on transparent background, square viewBox (e.g. 32x32), rounded square bg if desired but stroke/fill must be `#FFFFFF`. Simple path: rounded rect + "in" letters. No LinkedIn trademark text; just the mark glyph, "stylistically similar, legally distinct" per REQ-M-044.
    2. In `render-chrome.js renderTopNav()`, load the logo via `loadImage(join(assetsDir, 'linkedin-logo.svg'))` and draw it at top-left of the 1920x52 nav bar: x=20, y=10, 32x32. Draw BEFORE nav labels so labels remain readable.
    3. Shift nav labels right if needed so they do not overlap the logo area (x >= 64). Keep label spacing consistent — adjust starting x, not inter-label gap.
    Addresses REQ-M-040, REQ-M-044.
  </action>
  <acceptance_criteria>
    - File `mirror-post/src/compositor/assets/linkedin-logo.svg` exists and contains `xmlns` and `#FFFFFF` (or `white`)
    - `grep -c "linkedin-logo" mirror-post/src/compositor/render-chrome.js` >= 1
    - `grep "loadImage.*linkedin-logo" mirror-post/src/compositor/render-chrome.js` matches
    - No nav label overlaps logo region in rendered output (verified visually at Wave 5)
  </acceptance_criteria>
  <verify>
    <automated>test -f mirror-post/src/compositor/assets/linkedin-logo.svg && grep -q "xmlns" mirror-post/src/compositor/assets/linkedin-logo.svg && grep -q "linkedin-logo" mirror-post/src/compositor/render-chrome.js</automated>
  </verify>
  <done>Logo SVG exists; render-chrome.js loads and draws it top-left of nav bar; nav labels still render without overlap.</done>
</task>

<task type="auto">
  <name>Task 3: Fix reaction emoji rendering via Noto Color Emoji (Bug 3)</name>
  <files>
    mirror-post/src/compositor/fonts/NotoColorEmoji.ttf
    mirror-post/src/compositor/render-chrome.js
  </files>
  <read_first>mirror-post/src/compositor/render-chrome.js</read_first>
  <action>
    Root cause (diagnosed): `render-chrome.js` lines ~220-245 draw a colored background circle then call `ctx.fillText(reactionDef.emoji, cx, cy)` with `ctx.font = '16px Inter'`. Inter has no color emoji glyphs and node-canvas Cairo fallback does not reach system emoji fonts when only Inter is registered. Result: circles render, glyphs do not.

    Fix:
    1. Bundle `NotoColorEmoji.ttf` into `mirror-post/src/compositor/fonts/NotoColorEmoji.ttf`. Download from Google Noto repo (Apache 2.0, redistributable). Commit the TTF.
    2. In `render-chrome.js` near the existing `registerFont` Inter calls (lines ~20-22), add:
       `registerFont(join(fontsDir, 'NotoColorEmoji.ttf'), { family: 'Noto Color Emoji' });`
    3. At the emoji draw call (the `ctx.fillText(reactionDef.emoji, ...)` near line 241), set `ctx.font = '16px "Noto Color Emoji"'` JUST for that call, then restore to Inter for any subsequent text (counts, labels). Do NOT change the font used for the "1,247" reaction count or "89 comments" text.
    4. Keep the colored circle background — it is a LinkedIn visual convention.

    Addresses REQ-M-040. Gap reason: reaction emojis invisible in golden output.
  </action>
  <acceptance_criteria>
    - File `mirror-post/src/compositor/fonts/NotoColorEmoji.ttf` exists and is > 100KB
    - `grep -c "Noto Color Emoji" mirror-post/src/compositor/render-chrome.js` >= 2 (registerFont + fillText font stack)
    - `grep "registerFont.*NotoColorEmoji" mirror-post/src/compositor/render-chrome.js` matches
    - Chrome test suite still passes (emoji glyph may change pixel bytes — golden regeneration in Wave 4 handles this)
  </acceptance_criteria>
  <verify>
    <automated>cd mirror-post && test -f src/compositor/fonts/NotoColorEmoji.ttf && [ $(wc -c < src/compositor/fonts/NotoColorEmoji.ttf) -gt 100000 ] && grep -q "Noto Color Emoji" src/compositor/render-chrome.js</automated>
  </verify>
  <done>NotoColorEmoji.ttf bundled and registered; emoji fillText uses `"Noto Color Emoji"` font stack; Inter still used for labels and counts.</done>
</task>

<!-- ============================================================ -->
<!-- WAVE 2: nav_easter_eggs removal (parallel, independent files) -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 4 (Removal A): Remove nav_easter_eggs from brent-vellum fixture</name>
  <files>mirror-post/test/fixtures/expected-briefs/brent-vellum.json</files>
  <read_first>mirror-post/test/fixtures/expected-briefs/brent-vellum.json</read_first>
  <action>
    Delete the `"nav_easter_eggs": ["Evanrations", "Commints", "Messanging"]` key from the JSON at line ~63. Ensure valid JSON (no trailing comma). Do not touch any other fixture — trevor-hustle, trevor-closer, pete-titles are already clean.
  </action>
  <acceptance_criteria>
    - `grep -c "nav_easter_eggs" mirror-post/test/fixtures/expected-briefs/brent-vellum.json` returns 0
    - `node -e "JSON.parse(require('fs').readFileSync('mirror-post/test/fixtures/expected-briefs/brent-vellum.json','utf8'))"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd mirror-post && node -e "JSON.parse(require('fs').readFileSync('test/fixtures/expected-briefs/brent-vellum.json','utf8'))" && ! grep -q nav_easter_eggs test/fixtures/expected-briefs/brent-vellum.json</automated>
  </verify>
  <done>brent-vellum.json parses as valid JSON with no nav_easter_eggs key.</done>
</task>

<task type="auto">
  <name>Task 5 (Removal B): Remove nav_easter_eggs from Post Brief schemas</name>
  <files>
    mirror-post/src/brief/schema.js
    mirror-post/src/brief/structured-outputs-schema.js
  </files>
  <read_first>
    mirror-post/src/brief/schema.js
    mirror-post/src/brief/structured-outputs-schema.js
  </read_first>
  <action>
    1. `schema.js`:
       - Change `optional_keys: ["nav_easter_eggs"]` to `optional_keys: []`
       - Delete the validation branch at lines ~52-55 that checks `brief.nav_easter_eggs !== undefined && !isArr(...)`
    2. `structured-outputs-schema.js`:
       - Delete the `nav_easter_eggs: { type: "array", items: { type: "string" } }` property (lines ~128-131)
       - Change `OPTIONAL_PARAM_COUNT = 1` to `OPTIONAL_PARAM_COUNT = 0`
       - Leave `DECISION_GATE` logic unchanged (still PASS because 0 <= 24)
  </action>
  <acceptance_criteria>
    - `grep -rn "nav_easter_eggs" mirror-post/src/brief/` returns 0 matches
    - `grep "OPTIONAL_PARAM_COUNT = 0" mirror-post/src/brief/structured-outputs-schema.js` matches
  </acceptance_criteria>
  <verify>
    <automated>cd mirror-post && ! grep -rq nav_easter_eggs src/brief/ && grep -q "OPTIONAL_PARAM_COUNT = 0" src/brief/structured-outputs-schema.js</automated>
  </verify>
  <done>Both schemas free of nav_easter_eggs; OPTIONAL_PARAM_COUNT decremented to 0; decision gate still PASS.</done>
</task>

<task type="auto">
  <name>Task 6 (Removal C): Clean nav misspellings from persona voice distillate</name>
  <files>mirror-post/src/brief/prompts/persona-voice-distillate.md</files>
  <read_first>mirror-post/src/brief/prompts/persona-voice-distillate.md</read_first>
  <action>
    Remove any instruction or example in the distillate that directs the model to produce satirical nav-bar misspellings (Evanrations, Commints, Hone, Jabs, Messanging, etc.) or that references a `nav_easter_eggs` output field. Delete the whole section if it is dedicated to this mechanic; otherwise surgically remove the lines.

    DO NOT bump `derived_from_spec_version`. The source spec `mirror_pete.v2.json` is immutable (REQ-X-001) and is NOT being touched — this is a non-versioned text cleanup of a derived prompt artifact.
  </action>
  <acceptance_criteria>
    - `grep -E "nav_easter_eggs|Evanrations|Commints|Messanging|\\bHone\\b|\\bJabs\\b" mirror-post/src/brief/prompts/persona-voice-distillate.md` returns 0 matches
    - `derived_from_spec_version` in distillate unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd mirror-post && ! grep -Eq "nav_easter_eggs|Evanrations|Commints|Messanging" src/brief/prompts/persona-voice-distillate.md</automated>
  </verify>
  <done>Distillate free of nav misspelling mechanic; spec version unchanged.</done>
</task>

<task type="auto">
  <name>Task 7 (Removal D): Remove nav_easter_eggs from system prompt builder</name>
  <files>mirror-post/src/brief/prompts/system-prompt-builder.js</files>
  <read_first>mirror-post/src/brief/prompts/system-prompt-builder.js</read_first>
  <action>
    Delete the two references to `nav_easter_eggs` (schema description line ~32 and optional-keys enumeration line ~53). If an entire optional-keys block becomes empty, collapse it cleanly so the assembled prompt string remains well-formed.
  </action>
  <acceptance_criteria>
    - `grep -c "nav_easter_eggs" mirror-post/src/brief/prompts/system-prompt-builder.js` returns 0
    - Module still imports and runs: `node -e "import('./src/brief/prompts/system-prompt-builder.js')"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd mirror-post && ! grep -q nav_easter_eggs src/brief/prompts/system-prompt-builder.js && node -e "import('./src/brief/prompts/system-prompt-builder.js').then(()=>process.exit(0)).catch(()=>process.exit(1))"</automated>
  </verify>
  <done>system-prompt-builder.js free of nav_easter_eggs and still loads as ESM.</done>
</task>

<task type="auto">
  <name>Task 8 (Removal F): Verify REQUIREMENTS.md is clean of nav_easter_eggs</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>
    Planner-verified: `grep nav_easter_eggs .planning/REQUIREMENTS.md` returns 0 matches. This task is verify-only. If the grep unexpectedly finds a match, delete the referencing requirement text and note in commit message. Otherwise this task is a no-op verification commit (or folded into Task 9's regeneration commit).
  </action>
  <acceptance_criteria>
    - `grep -c "nav_easter_eggs" .planning/REQUIREMENTS.md` returns 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -q nav_easter_eggs .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md confirmed clean.</done>
</task>

<!-- ============================================================ -->
<!-- WAVE 3: Dependent removals (render-chrome + chrome tests)    -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 9 (Removal E): Hardcode nav labels in render-chrome.js</name>
  <files>mirror-post/src/compositor/render-chrome.js</files>
  <read_first>mirror-post/src/compositor/render-chrome.js</read_first>
  <action>
    Depends on Tasks 2 and 3 (same file). After logo + emoji changes land, modify nav label logic:
    1. Set `NAV_LABELS = ['Home', 'My Network', 'Jobs', 'Messaging', 'Notifications']` (note: 'My Network' with space, not 'Network').
    2. Delete the conditional at lines ~83-85 that reads `brief.nav_easter_eggs`. Replace with direct use of `NAV_LABELS`.
    3. Do not accept any `nav_easter_eggs` input anywhere in this file.
  </action>
  <acceptance_criteria>
    - `grep -c "nav_easter_eggs" mirror-post/src/compositor/render-chrome.js` returns 0
    - `grep "My Network" mirror-post/src/compositor/render-chrome.js` matches
    - `grep "'Home', 'My Network', 'Jobs', 'Messaging', 'Notifications'" mirror-post/src/compositor/render-chrome.js` matches
  </acceptance_criteria>
  <verify>
    <automated>cd mirror-post && ! grep -q nav_easter_eggs src/compositor/render-chrome.js && grep -q "My Network" src/compositor/render-chrome.js</automated>
  </verify>
  <done>render-chrome.js renders fixed labels `[Home, My Network, Jobs, Messaging, Notifications]` regardless of brief contents.</done>
</task>

<task type="auto">
  <name>Task 10 (Removal G): Update compositor-chrome tests for fixed nav labels</name>
  <files>mirror-post/test/compositor-chrome.js</files>
  <read_first>mirror-post/test/compositor-chrome.js</read_first>
  <action>
    Depends on Tasks 4, 5, 9. Update any test that asserts `nav_easter_eggs` behavior to instead assert that `NAV_LABELS` always renders the fixed labels even when the brief has no easter-egg field. NEVER skip or disable a test. If a test name mentions easter eggs, rename it to describe the hardcoded behavior. Also update any test that asserted orange `#0b5ca8` to assert LinkedIn blue `#0A66C2`.
  </action>
  <acceptance_criteria>
    - `grep -c "nav_easter_eggs" mirror-post/test/compositor-chrome.js` returns 0
    - `grep -c "0b5ca8" mirror-post/test/compositor-chrome.js` returns 0
    - No `.skip(` or `xtest(` added
    - `npm run test:compositor` passes all compositor-chrome tests
  </acceptance_criteria>
  <verify>
    <automated>cd mirror-post && ! grep -q nav_easter_eggs test/compositor-chrome.js && ! grep -q "0b5ca8" test/compositor-chrome.js && npm run test:compositor</automated>
  </verify>
  <done>compositor-chrome tests updated; all pass; zero skipped.</done>
</task>

<!-- ============================================================ -->
<!-- WAVE 4: Regenerate golden + full regression                   -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task 11: Regenerate golden PNG and run full regression</name>
  <files>mirror-post/test/fixtures/golden-outputs/brent-vellum-golden.png</files>
  <read_first>mirror-post/scripts/generate-golden.mjs</read_first>
  <action>
    Depends on Tasks 1-10. Run:
    1. `cd mirror-post && node scripts/generate-golden.mjs` — regenerates `test/fixtures/golden-outputs/brent-vellum-golden.png` with new LinkedIn-blue chrome, logo, and color emoji.
    2. `git add test/fixtures/golden-outputs/brent-vellum-golden.png` — commit the new deterministic anchor.
    3. `npm run test:all` — must exit 0, 187+ tests, 0 failing, 0 skipped. Golden byte-match (G3) must pass against the just-committed PNG. If any determinism test fails cross-run, investigate non-determinism before proceeding.
    Addresses REQ-X-052.
  </action>
  <acceptance_criteria>
    - `mirror-post/test/fixtures/golden-outputs/brent-vellum-golden.png` is a valid PNG (file command reports PNG)
    - `git ls-files mirror-post/test/fixtures/golden-outputs/brent-vellum-golden.png` returns the path
    - `cd mirror-post && npm run test:all` exits 0
    - Test count >= 187, failing = 0, skipped = 0
  </acceptance_criteria>
  <verify>
    <automated>cd mirror-post && node scripts/generate-golden.mjs && file test/fixtures/golden-outputs/brent-vellum-golden.png | grep -q PNG && npm run test:all</automated>
  </verify>
  <done>Golden regenerated, committed, and full regression green.</done>
</task>

<!-- ============================================================ -->
<!-- WAVE 5: Human UAT gate — DO NOT close Phase 4                 -->
<!-- ============================================================ -->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 12: Human visual UAT on regenerated golden</name>
  <what-built>
    - LinkedIn-blue chrome (#0A66C2) across top nav, profile bar, avatar
    - LinkedIn "in" logo top-left of nav bar
    - Reaction emoji glyphs rendered via Noto Color Emoji over colored circles
    - Nav labels fixed: Home, My Network, Jobs, Messaging, Notifications
    - `nav_easter_eggs` removed cross-phase
    - Regenerated `brent-vellum-golden.png`
  </what-built>
  <how-to-verify>
    1. Open `mirror-post/test/fixtures/golden-outputs/brent-vellum-golden.png`.
    2. Confirm top nav and profile bar are LinkedIn blue (#0A66C2), not orange.
    3. Confirm "in" logo is visible top-left of nav bar.
    4. Confirm reaction bar shows actual emoji glyphs (thumbs-up, heart, clap, etc.) over colored circles — not empty circles.
    5. Confirm avatar circle is LinkedIn blue matching chrome.
    6. Confirm nav labels read exactly: Home, My Network, Jobs, Messaging, Notifications (no misspellings).
    7. Confirm tweet card accent and verify badge are bright blue #1D9BF0 (unchanged).
  </how-to-verify>
  <resume-signal>
    Type "approved" to advance. If any visual issue remains, describe it; planner will open a follow-up gap-closure plan.
    DO NOT run `/gsd:verify-work` to close Phase 4 from this plan. Phase 4 closure happens in a later pass after operator approval.
  </resume-signal>
  <done>Operator has visually inspected the regenerated golden and approved or listed remaining issues.</done>
</task>

</tasks>

<verification>
- `grep -r "nav_easter_eggs" mirror-post/src/ mirror-post/test/ .planning/REQUIREMENTS.md` -> 0 matches
- `grep -rE "Evanrations|Commints|Messanging" mirror-post/src/ mirror-post/test/` -> 0 matches
- `grep -r "0b5ca8" mirror-post/src/compositor/ mirror-post/test/compositor-chrome.js` -> 0 matches
- `grep -r "0A66C2" mirror-post/src/compositor/constants.js` -> >= 3 matches
- `test -f mirror-post/src/compositor/assets/linkedin-logo.svg`
- `test -f mirror-post/src/compositor/fonts/NotoColorEmoji.ttf`
- `cd mirror-post && npm run test:all` exits 0 with >= 187 tests, 0 failing, 0 skipped
</verification>

<success_criteria>
- All 5 visual bugs closed (chrome color, logo, emoji, avatar, nav labels)
- `nav_easter_eggs` field removed from schemas, fixtures, prompts, compositor, tests
- Golden PNG regenerated, committed, byte-match test passes
- Full regression green (>= 187 tests, 0 failing, 0 skipped)
- Operator visually inspected regenerated golden at Wave 5 checkpoint
- Phase 4 NOT closed — awaits separate verification pass after human approval
</success_criteria>

<output>
After completion, create `.planning/phases/04-compositor/04-03-SUMMARY.md` documenting: commits made, final test counts, golden PNG hash, and any residual visual issues surfaced at the human UAT gate.
</output>
