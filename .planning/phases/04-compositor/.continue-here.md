---
phase: 04-compositor (04.2 rescope)
task: spawn-planner (not started)
total_tasks: 3 (planner spawn, verify, requirements coverage)
status: paused_pre_plan
last_updated: 2026-04-15T17:59:49.233Z
---

<current_state>
Plan-phase workflow for Phase 04.2 was prepped but context exhausted at 90% before the gsd-planner subagent could be spawned. All inputs are loaded and canonical — CONTEXT.md has 14 locked decisions (D-NEW-01..14), DISCUSSION-LOG has full audit trail across three sections (A original, B advisor AM, C resume PM), ROADMAP 04.2 entry lists success criteria and pending plans. Working tree is clean. Commit e266f22 captured the writeup.
</current_state>

<completed_work>

- Loaded `.planning/phases/04-compositor/04-CONTEXT.md` (14 D-NEW decisions, survives/dies/new inventory, composite layering order)
- Loaded `.planning/phases/04-compositor/04-DISCUSSION-LOG.md` (Section A/B/C audit trail)
- Loaded `.planning/STATE.md` (status: context_captured_ready_for_plan)
- Loaded `tasks/lessons.md` (2026-04-15 architecture + determinism rules active)
- Surveyed ROADMAP Phase 04.2 entry (goal, REQ-IDs, retired requirements, Phase 5/6/7 renumbering)
- Surveyed surviving compositor source: `mirror-post/src/compositor/{composite.js, render-text-overlay.js, constants.js, index.js}` confirmed present; dying modules (`render-chrome.js`, `render-tweet-card.js`) still on main pending D-NEW-13 deletion after plan approval
- Read plan-phase workflow from `~/.claude/get-shit-done/workflows/plan-phase.md` in full
</completed_work>

<remaining_work>

- **Spawn gsd-planner** with prompt referencing: 04-CONTEXT.md (authoritative), 04-DISCUSSION-LOG.md (audit), ROADMAP Phase 04.2, REQUIREMENTS.md (REQ-M-041/042, REQ-X-052b, REQ-X-060, REQ-X-061..066), surviving source paths. Enforce that every task ships with `<read_first>` + `<acceptance_criteria>` blocks per plan-phase deep_work_rules. Planner must produce 5 plans:
  1. `04.2-01-PLAN.md` — Post Brief v2 schema migration (sub-plan per D-NEW-14). Adds nullable override fields, `image_output.scene_png`. Migrates 4 v1 fixtures. v1 validator coexists during migration window.
  2. `04.2-02-PLAN.md` — VARIANT_SLOTS constants + barrel rewrite (D-NEW-03). Replaces NAV_LABELS/REACTION_EMOJI. Coordinates schema: `{headline|body: {x,y,width,height,fontSize,lineHeight,maxLines,fontWeight,color}}` per variant A/B/C/D.
  3. `04.2-03-PLAN.md` — Legacy module deletion (D-NEW-13 timing: after plan approval per FROZEN-05). Removes render-chrome.js, render-engagement-bar.js, render-tweet-card.js, nav-icon module, emoji pipeline, BGRA channel-swap patch, old golden PNG, Twemoji NotoColorEmoji.ttf font bundle.
  4. `04.2-04-PLAN.md` — compositePost() rewrite (D-NEW-01/02). Three-layer composite: load scene PNG from `brief.image_output.scene_png` → overlay `chrome-assets/variant-{X}.png` → render text into `VARIANT_SLOTS[variant]` via buildStyledSegments() (which survives). Archetype→variant mapping with B as fallback (D-NEW-05).
  5. `04.2-05-PLAN.md` — Golden test against `test/fixtures/scenes/variant-a.png` for REQ-X-052b determinism. Execution-blocked on Phase 5a landing the fixture; plan is writable now, test_runner invocation is gated.
- **Verify plans** via gsd-verifier (scope: plan). Up to 3 revision iterations if ISSUES FOUND.
- **Requirements coverage gate** — cross-check REQ-M-041, REQ-M-042, REQ-X-052b, REQ-X-060, REQ-X-061..066 against plan frontmatter `requirements:` fields.
</remaining_work>

<decisions_made>

- **Skip research flag required on retry.** Advisor-researcher subagents already ran during discuss-phase (DISCUSSION-LOG Section B). CONTEXT is exhaustive. Research would burn context duplicating locked decisions. Use `--skip-research`.
- **5-plan breakdown is the right grain.** Not 1 (too monolithic, can't parallelize), not 10 (every plan has meaningful independent test surface). Matches D-NEW-14 (v2 sub-plan), D-NEW-03 (constants), D-NEW-13 (deletion), D-NEW-01/02 (composite rewrite), REQ-X-052b (golden test).
- **Chrome PNG authoring is operator handoff, NOT a Claude task.** D-NEW-09 explicitly: operator generates the 4 PNGs via flux-krea + masking between plan-phase approval and execute-phase start. Plan tasks reference the PNGs as inputs.
- **Golden test plan is writable even though fixture is Phase 5a dependency.** Plan 04.2-05 authors the test harness now; execution is gated on `mirror-post/test/fixtures/scenes/variant-a.png` arriving from Phase 5a. Mark this explicitly in the plan's blockers.
</decisions_made>

<blockers>

- **Context exhausted at 90%**: Technical blocker. Resolved by restart with `--skip-research`.
- **Phase 5a scene fixture pending**: Soft blocker on 04.2-05-PLAN execution only, not on plan authoring. Phase 5 split (D-NEW-12) exists precisely so 5a can ship variant-a.png without waiting on 5b.
</blockers>

<context>
The rescope was expensive — 14 decisions, three discussion sessions, ROADMAP renumbering, atomic commit. The plan-phase must not re-litigate any of those decisions. Planner gets CONTEXT.md as authoritative input and produces plans that *execute* those decisions. Any planner output that contradicts a D-NEW decision is a verification failure.

Every task frontmatter must include `read_first:` pointing at CONTEXT.md and relevant surviving source. Every task must have `acceptance_criteria:` — plan-phase workflow treats both as non-optional.

REQ-X-052b is the sharpest acceptance bar: overlay determinism — byte-identical PNG output given identical scene+chrome+brief inputs. This scoped to Phase 4 specifically (REQ-X-052a stays on Phase 5 scene generation) per lessons.md 2026-04-15 determinism rule.

Deletion plan (04.2-03) must run AFTER the composite rewrite (04.2-04) passes verification in the execute-phase wave plan — otherwise the rewrite can't cross-reference the old implementation. Planner should encode this as `depends_on: 04.2-04` on the deletion plan.
</context>

<next_action>
Fresh session: run `/gsd:pause-work` confirmation (if not already committed), then `/clear`, then:

```
/gsd:plan-phase 04.2 --skip-research
```

Planner prompt must reference:
- `.planning/phases/04-compositor/04-CONTEXT.md` (authoritative, 14 decisions)
- `.planning/phases/04-compositor/04-DISCUSSION-LOG.md` (audit only)
- `.planning/ROADMAP.md` Phase 04.2 entry
- `.planning/REQUIREMENTS.md` (REQ-M-041, REQ-M-042, REQ-X-052b, REQ-X-060, REQ-X-061..066)
- Surviving source: `mirror-post/src/compositor/{composite.js, render-text-overlay.js, constants.js, index.js}` and `mirror-post/src/brief/schema.js`
- Dying source (for deletion plan only): list in 04-CONTEXT.md code_context section

Target output: 5 plans in `.planning/phases/04-compositor/04.2-01..05-PLAN.md` with full `<read_first>` + `<acceptance_criteria>` blocks.
</next_action>
