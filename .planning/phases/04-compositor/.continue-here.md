---
phase: 04-compositor
plan: 04.2 (writeup pending — all decisions frozen)
task: 0
total_tasks: 0
status: decisions_frozen_ready_for_writeup
last_updated: 2026-04-15T18:05:00.000Z
supersedes: previous .continue-here.md (2026-04-15T16:35:00.000Z) — operator resolved all six carry-over blockers inline on resume
---

<current_state>
Resume session (2026-04-15 PM). Operator read the prior checkpoint, chose Option 1 (resume + write), and resolved ALL six carry-over blockers inline with explicit answers. Context reached 87% before any artifacts were written; paused cleanly via /gsd:pause-work. This file now carries the frozen operator answers as authoritative input to next session's writeup — NOT open questions. Next session does NOT need to ask the operator anything before writing 04-CONTEXT.md and 04-DISCUSSION-LOG.md.

Lessons commit landed this session: `4e86c2d` — docs(lessons): capture architecture-review + determinism rules from Phase 4 RESET. Working tree otherwise clean except this checkpoint file.
</current_state>

<frozen_decisions>

## Decisions captured in prior discuss-phase (2026-04-15 AM)

**DECISION 1 — A+B boundary: Compositor owns chrome PNG (B's approach).**
- Phase 5 generates scene-only PNG (hero figure + environment/office; NO chrome, NO nav, NO mini card, NO reaction row, NO comment group).
- Phase 4 composites a static per-variant chrome PNG (A/B/C/D, transparent where scene shows through) over the Phase 5 scene, then overlays text into VARIANT_SLOTS coordinates.
- Chrome PNG assets are a Phase 04.2 prerequisite.
- REQ-X-052b (overlay determinism) now guards chrome-overlay + text-overlay composite. Scene determinism is REQ-X-052a (Phase 5).
- **This REVISES D-NEW-01 from 04-CONTEXT-RESET.md** (chrome ownership walks back from Phase 5 to Phase 4). Rest of D-NEW-01 (scene pipeline in Phase 5) stands.

**DECISION 2 — Schema v2 bump (advisor C recommendations accepted as bundle).**
- C1: `image_seed.environment` / `subject_pose` / `mood` / `color_temperature` survive as optional nullable overrides in v2. Default source = variant template; null = use template default.
- C2: Placeholder substitution (`[Profile Name]`, `[Board Title]`, etc.) happens at Phase 5 render time — not Phase 2, not Phase 4.
- C3: Post Brief gains `image_output.scene_png` field carrying the scene path. Phase 4 reads path directly, no cache lookup.
- Migration: 4 existing v1 fixtures migrated to v2, `schema_version` bumps `"1.0"` → `"2.0"`.
- **This RESOLVES D-NEW-06** (v2 bump confirmed).

## Six carry-over blockers resolved this session (2026-04-15 PM)

**FROZEN-01 — D-NEW-01 reversal CONFIRMED.** Chrome moves Phase 5 → Phase 4 as static per-variant PNG overlays. Intentional. Scene pixels (office, character, props) stay Phase 5. Chrome UI (nav bar, logo, post header frame, mini-card border, reaction row shapes) is Phase 4's static asset.

**FROZEN-02 — Chrome PNG authoring contract.**
- Authoring method: flux-krea generates 4 reference scenes (one per variant A/B/C/D); scene region masked out; chrome frozen as transparent-layer PNG.
- Resolution: **1920×1080 RGBA** (matches REQ-X-060 horizontal 16:9).
- Transparency: scene zone transparent, chrome opaque. Masking via rembg or sharp-based background removal. Operator QA before commit.
- Repo location: `mirror-post/src/compositor/chrome-assets/` — filenames `variant-a.png`, `variant-b.png`, `variant-c.png`, `variant-d.png`.
- Version control: committed to mirror-post repo. VARIANT_SLOTS coordinates versioned alongside as `constants.js`.
- Authoring sequence: task **inside Phase 04.2 plan** — operator generates via flux-krea, hands off finished PNGs to plan execution.
- Regeneration trigger: only if variant prompt changes substantively; otherwise frozen forever.

**FROZEN-03 — ROADMAP renumbering.**
- Phase 4: Compositor (current, rescoped to chrome PNG overlay + text)
- Phase 5: Image Prompt Engine (was "Artifact UI")
- Phase 6: Artifact UI (shifted from Phase 5)
- Phase 7: Integration (shifted from Phase 6)
- Apply to ROADMAP.md + STATE.md as part of next session's atomic commit.

**FROZEN-04 — Phase 5 split (5a/5b).**
- Phase 5a: variant A end-to-end (prompt engine + variant A scene generator + first scene PNG fixture). Unblocks Phase 04.2 execution sooner.
- Phase 5b: variants B/C/D + remaining Image Prompt Engine scope.
- Rationale: one variant scene fixture unblocks Phase 04.2's golden test; waiting for all four serializes.

**FROZEN-05 — Deprecated module deletion timing.**
- Delete from `main` AFTER Phase 04.2 plan is approved — not before.
- Rationale: plan-phase references "what's being deleted" when scoping survivors. Preserved on `archive/phase-4-programmatic-chrome` (mirror-post SHA `c25d88a`) in the meantime.

**FROZEN-06 — Phase 2 v2 migration scoping.**
- Handle as sub-plan **inside Phase 04.2 execution**, NOT a separate Phase 02-revisit.
- Rationale: schema change is motivated by Phase 04.2 decisions, not general Phase 2 evolution. Keep coupled.
- Sub-plan includes: `schema.js` v2 update, 4 fixture migrations, validator update, compatibility note in CLAUDE.md.

</frozen_decisions>

<remaining_work>

Next session (fresh context) executes this in order — all answers live above, no operator input needed until the atomic commit lands:

1. [ ] Read `.planning/phases/04-compositor/04-CONTEXT-RESET.md` (already on disk)
2. [ ] Read `.planning/phases/04-compositor/04-CONTEXT.md` (original, preserved)
3. [ ] Read `$HOME/.claude/get-shit-done/templates/context.md` for CONTEXT template structure
4. [ ] **Write `.planning/phases/04-compositor/04-CONTEXT.md`** (supersedes both prior versions). Required sections and content:
   - `<domain>`: Compositor = static per-variant chrome PNG overlay + text overlay onto Phase 5 scene PNG. Input: scene PNG from `image_output.scene_png` + Post Brief v2 + one of four chrome PNGs. Output: final 1920×1080 PNG.
   - `<decisions>`:
     - **D-NEW-01 (REVISED — FROZEN-01)**: chrome ownership is Phase 4, scene is Phase 5. Note revision explicitly.
     - **D-NEW-02** (import unchanged): compositor renders headline + body text only.
     - **D-NEW-03** (import unchanged): VARIANT_SLOTS constants per variant in code.
     - **D-NEW-04** (import unchanged): REQ-X-052 split into 052a (scene, Phase 5) + 052b (overlay, Phase 4).
     - **D-NEW-05** (import unchanged): archetype → variant static table, fallback B, enum in Post Brief.
     - **D-NEW-06 (RESOLVED — DECISION 2)**: Post Brief schema v2 bump confirmed. C1/C2/C3 details above.
     - **D-NEW-07** (import unchanged): legacy modules deleted from main. Timing: after 04.2 plan approved (FROZEN-05).
     - **D-NEW-08** (import unchanged): `nav_easter_eggs` removal retroactively justified.
     - **D-NEW-09 (NEW — FROZEN-02)**: chrome PNG authoring contract. 1920×1080 RGBA, `mirror-post/src/compositor/chrome-assets/variant-{a,b,c,d}.png`, flux-krea + rembg/sharp masking pipeline, task inside Phase 04.2 plan, regenerate only on prompt change.
     - **D-NEW-10 (NEW — DECISION 2 C2)**: placeholder substitution is Phase 5 render-time responsibility. Phase 4 receives finished scene pixels.
     - **D-NEW-11 (NEW — FROZEN-03)**: ROADMAP renumbering. Phase 4 Compositor → Phase 5 Image Prompt Engine → Phase 6 Artifact UI → Phase 7 Integration.
     - **D-NEW-12 (NEW — FROZEN-04)**: Phase 5 split 5a (variant A) / 5b (B/C/D + rest).
     - **D-NEW-13 (NEW — FROZEN-05)**: deprecated module deletion after 04.2 plan approval, not before.
     - **D-NEW-14 (NEW — FROZEN-06)**: Post Brief v2 migration lives as sub-plan inside Phase 04.2 execution.
     - **Claude's Discretion**: masking tool choice (rembg vs sharp), exact VARIANT_SLOTS coordinates (operator measures per chrome PNG before execution), file layout within `src/compositor/` beyond the two prescribed subpaths.
   - `<canonical_refs>` (MANDATORY): full paths to
     - `.planning/PROJECT.md`
     - `.planning/REQUIREMENTS.md`
     - `.planning/ROADMAP.md`
     - `.planning/STATE.md`
     - `.planning/phases/04-compositor/04-CONTEXT-RESET.md`
     - `.planning/phases/04-compositor/04-CONTEXT.md` (original, preserved)
     - `.planning/phases/05-image-prompt-engine/templates/linkedin-template-variants-master-wrapper.md`
     - `.planning/phases/05-image-prompt-engine/templates/linkedin-template-variant-a-startup-glass-desk.md`
     - `.planning/phases/05-image-prompt-engine/templates/linkedin-template-variant-b-executive-strategy-office.md`
     - `.planning/phases/05-image-prompt-engine/templates/linkedin-template-variant-c-collaborative-creative-office.md`
     - `.planning/phases/05-image-prompt-engine/templates/linkedin-template-variant-d-sterile-analyst-war-room.md`
     - `mirror-post/src/persona/spec/mirror_pete.v2.json` (persona spec, REQ-X-001)
     - `mirror-post/src/brief/schema.js` (Post Brief schema target of v2 bump)
     - `mirror-post/src/grammar/zone-spec.json`, `mirror-post/src/grammar/engagement.js`, `mirror-post/src/grammar/prop-taxonomy.js`
   - `<code_context>`:
     - SURVIVES: `render-text-overlay.js`, `buildStyledSegments`, shrunk `compose.js`.
     - DIES (after 04.2 plan approval, FROZEN-05): `render-chrome.js`, `render-engagement-bar.js`, `render-tweet-card.js`, nav-icon module, emoji pipeline, BGRA channel-swap fix, NAV_LABELS, REACTION_EMOJI map.
     - NEW: `src/compositor/chrome-assets/` (4 PNGs), `src/compositor/constants.js` (VARIANT_SLOTS), scene fixture loader reading `test/fixtures/scenes/variant-{a,b,c,d}.png`.
     - Integration: `compositor/index.js` barrel exports `compositePost()` — accepts Post Brief v2 (with `image_output.scene_png`) + returns 1920×1080 PNG buffer.
   - `<specifics>`:
     - Chrome PNG dimensions: 1920×1080 RGBA, scene zone transparent, chrome opaque.
     - Composite layering order (bottom → top): scene PNG (from `image_output.scene_png`) → chrome PNG (variant-{a|b|c|d}.png) → text overlay layer (headline + body into VARIANT_SLOTS[variant]).
     - VARIANT_SLOTS coordinate format: `{ headline: {x, y, width, height, fontSize, lineHeight, maxLines, fontWeight, color}, body: {x, y, width, height, fontSize, lineHeight, maxLines, fontWeight, color} }`.
     - Scene PNG dimensions: 1920×1080 (matches chrome RGBA). Diffusion owns all pixels except where chrome opaque.
     - Determinism: REQ-X-052b = same Post Brief v2 + same scene PNG + same chrome PNG + same VARIANT_SLOTS version → byte-identical output.
   - **NO `<deferred>` section with open blockers** — all six are resolved. Only "deferred" items are classic scope cuts (AI-generated avatars, custom SVG icons, tweet timestamp gilding — if relevant to the new scope at all; most were killed with chrome).
5. [ ] **Write `.planning/phases/04-compositor/04-DISCUSSION-LOG.md`** — audit trail. Structure:
   - Session 1 (AM): discuss-phase with 3 advisor-researcher subagents (A/B/C). Bullet each advisor's recommendation. Log the A/B collision. Log operator's A+B pick (DECISION 1) and Schema v2 pick (DECISION 2). Note this REVISED D-NEW-01 and RESOLVED D-NEW-06.
   - Session 2 (PM): resume-work. Operator resolved six carry-over blockers inline (FROZEN-01 through FROZEN-06). List each with operator's one-line rationale. Note D-NEW-09 through D-NEW-14 were born here.
6. [ ] **Update `.planning/ROADMAP.md`** with FROZEN-03 renumbering. Phase 4 rescoped description. Phase 5 becomes Image Prompt Engine (split 5a/5b per FROZEN-04). Phase 6 = Artifact UI. Phase 7 = Integration. Update coverage table REQ-ID column accordingly. Update progress table rows.
7. [ ] **Update `.planning/STATE.md`** — `status: context_captured_ready_for_plan`. Session continuity reflects six inline decisions + writeup commit.
8. [ ] **Atomic commit** (single commit for steps 4–7 + delete this `.continue-here.md` + delete `.planning/HANDOFF.json` as consumed one-shot artifacts):
   ```
   docs(04.2): capture phase context, discussion log, and ROADMAP renumbering

   - Write 04-CONTEXT.md (supersedes 04-CONTEXT-RESET.md + original 04-CONTEXT.md)
     with 14 decisions: D-NEW-01..08 from RESET (01 revised, 06 resolved) +
     D-NEW-09..14 from resume-session inline resolution
   - Write 04-DISCUSSION-LOG.md audit trail (two sessions, two decision waves)
   - Renumber ROADMAP: P4 Compositor / P5 Image Prompt Engine (5a+5b) /
     P6 Artifact UI / P7 Integration
   - Update STATE.md status → context_captured_ready_for_plan
   - Delete consumed checkpoint (.continue-here.md) and handoff (HANDOFF.json)

   Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
   ```
9. [ ] Notify operator: `/gsd:plan-phase 04.2` is unblocked. Flag that chrome PNG authoring (FROZEN-02) becomes a task inside the plan — operator generates the 4 chrome PNGs via flux-krea between plan-phase and execute-phase.

</remaining_work>

<blockers>

**None blocking writeup.** All six prior blockers resolved inline. Operator does not need to approve anything further before next session writes the artifacts — the writeup simply transcribes frozen decisions into canonical files.

After writeup commits, two items remain gated but are NOT blocking writeup:
- Chrome PNG authoring (FROZEN-02) — operator action, happens between `/gsd:plan-phase 04.2` and `/gsd:execute-phase 04.2`.
- Phase 5a (variant A) must produce at least one scene PNG fixture before Phase 04.2 golden test can be built. Normal sequencing; plan-phase can still run.

</blockers>

<context>
This session (resume) ran from /gsd:resume-work → Option 1 → operator provided complete six-blocker resolution block → I committed lessons (4e86c2d) and was mid-step-3 (CONTEXT.md write) when context hit 87%. Recommended pause; operator ran /gsd:pause-work.

The handoff design: next session starts at 0% context, reads RESET + original CONTEXT + template (all still ~small and cached locally), then writes approximately 500 lines of new artifacts (CONTEXT + DISCUSSION-LOG + ROADMAP edits + STATE edits) and commits atomically. Well within a fresh context budget.

Hard rules preserved from operator directive (still in force next session):
- Do NOT run /gsd:plan-phase 04.2 in the writeup session.
- Do NOT modify mirror-post code (no chrome PNG generation yet).
- Do NOT touch mirror_pete.v2.json (REQ-X-001).
- Do NOT revert or delete Phase 4 archive branch.

Mental model for next session: chrome ownership is Phase 4 (static PNG per variant), scene is Phase 5, Post Brief bumps to v2 carrying `image_output.scene_png`, ROADMAP renumbers, v2 migration is a 04.2 sub-plan, deprecated modules die after plan approval (not now).
</context>

<next_action>
Fresh session (recommend `/clear` first, then `/gsd:resume-work`). Resume-work will pick up this checkpoint, see `decisions_frozen_ready_for_writeup`, and route directly to writeup. No operator input needed until Step 9 (post-commit notification).

Expected duration next session: ~1500–2500 tokens of reads + ~500 lines of writes + one commit. Well under context budget.
</next_action>
