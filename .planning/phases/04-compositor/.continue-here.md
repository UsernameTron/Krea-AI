---
phase: 04-compositor
plan: 04.2 (architecture reset)
task: 0
total_tasks: 0
status: paused_awaiting_human_approval
last_updated: 2026-04-15T15:45:13.675Z
---

<current_state>
Phase 4 is mid-architecture-reset. Programmatic LinkedIn chrome rendering has been abandoned. The compositor's new contract is text overlay only onto AI-generated full-scene LinkedIn screenshots produced by Phase 5 (Image Prompt Engine). The reset CONTEXT doc, ROADMAP update, and STATE update are committed (34946c6). Five Phase 5 template variant prompts are committed (45dfdfa). Prior compositor work is preserved on mirror-post branch `archive/phase-4-programmatic-chrome` at SHA `c25d88a` — nothing reverted on main. The BGRA channel-swap bug fix is STOPPED and will not merge. We are blocked on operator approval of `04-CONTEXT-RESET.md` before any Phase 04.2 planning or execution.
</current_state>

<completed_work>

- [x] Archive prior Phase 4 work on `archive/phase-4-programmatic-chrome` (mirror-post SHA `c25d88a430f080c58854c16a248584c7ae245587`)
- [x] Commit Phase 5 LinkedIn template variants A/B/C/D + master wrapper to `.planning/phases/05-image-prompt-engine/templates/` (Krea-AI commit `45dfdfa`)
- [x] Write `.planning/phases/04-compositor/04-CONTEXT-RESET.md` capturing D-NEW-01..08, archetype→variant mapping for all 17 archetypes, what-survives-what-dies module table, and 6 risks
- [x] Mark Phase 4 RESET IN PROGRESS in `.planning/ROADMAP.md` and flag the Phase 5 naming collision (current "Phase 5: Artifact UI" vs new `05-image-prompt-engine/` directory) for operator decision
- [x] Update `.planning/STATE.md` status to `reset_in_progress` with full pivot summary
- [x] Commit reset state to Krea-AI main as `34946c6`
- [x] Capture two learned lessons in `tasks/lessons.md` (architecture symptom-patch limit; determinism boundary split) — uncommitted, awaiting operator review
</completed_work>

<remaining_work>

- [ ] **HUMAN: Operator approves `04-CONTEXT-RESET.md`** — gates everything below
- [ ] **HUMAN: Resolve ROADMAP renumbering** — Image Prompt Engine → Phase 5, Artifact UI → Phase 6, Integration → Phase 7 (or another scheme)
- [ ] **HUMAN: Decide Post Brief schema bump strategy (D-NEW-06)** — v2 bump (preferred) vs in-place v1 patch
- [ ] **HUMAN: Decide whether to split Phase 5** into 5a (variant A end-to-end) and 5b (B/C/D + rest) so Phase 04.2 can start as soon as one variant scene exists
- [ ] **HUMAN: Decide deprecated-module deletion timing** — delete from main now, or after 04.2 plan is approved
- [ ] Run `/gsd:plan-phase 04.2` (only after CONTEXT approval)
- [ ] Phase 5 must produce at least one variant scene PNG fixture before Phase 04.2 execution can build the new golden test
</remaining_work>

<decisions_made>

- **D-NEW-01** Scene generation (LinkedIn chrome, nav, mini post card, reaction row, comment group, in-scene office) belongs to Phase 5 — baked into the diffusion output
- **D-NEW-02** Compositor overlays text only; `render-text-overlay.js` survives, all chrome modules die
- **D-NEW-03** Per-variant `VARIANT_SLOTS` constants in code — rejected scene-aware CV positioning and flux-krea sidecar JSON (each variant has a fixed UI shell by template design, slots are pinned not floating)
- **D-NEW-04** Determinism contract split: REQ-X-052a (scene determinism, Phase 5, seed-deterministic) + REQ-X-052b (overlay determinism, Phase 4, byte-identical against checked-in scene PNG fixture)
- **D-NEW-05** Static archetype→variant table in Phase 2; `image_seed.scene_template` becomes enum `"A"|"B"|"C"|"D"`; fallback **B** (executive strategy office); distribution A=4, B=4, C=3, D=6
- **D-NEW-06** Post Brief schema bump to v2 preferred over in-place v1 patch (operator decision pending)
- **D-NEW-07** Legacy compositor modules deleted from main (preserved on archive branch only) — honors global No Orphaned Code rule
- **D-NEW-08** Cross-phase `nav_easter_eggs` removal retroactively justified — entire chrome path is gone, removal stands

Two new lessons captured (`tasks/lessons.md`, uncommitted):
- **[Architecture]** Three+ symptom patches in one module = mandatory architecture-review pause. Triggered by Phase 4's Twemoji COLR swap → nav_easter_eggs removal → golden PNG regen → BGRA channel-swap chain.
- **[Determinism]** Don't enforce byte-identical contracts across pipeline stages produced by different rendering engines. Split the determinism boundary at the seam with a versioned fixture handoff.
</decisions_made>

<blockers>
- **Operator approval of `04-CONTEXT-RESET.md`** — hard gate. Nothing in Phase 04.2 moves until this happens. Hard rule from the operator: do not auto-proceed to `/gsd:plan-phase 04.2`.
- **ROADMAP Phase 5 naming collision** — flagged in ROADMAP and STATE; not auto-fixed.
- **Phase 5 is now strictly blocking Phase 4** — at least one variant scene PNG fixture must exist before Phase 04.2 golden test can be built. Recommend Phase 5 split.
</blockers>

<context>
This was a hard architecture pivot triggered by the operator stopping a BGRA channel-swap bug fix mid-flight and calling the entire programmatic-chrome approach wrong. The reset is intentional and conservative: archive everything, commit the new template seeds, write a superseding CONTEXT (do not edit the old one), update ROADMAP/STATE, and STOP. No mirror-post code touched in this session. No commits reverted. The hard rules from the operator's instructions are sacred — do not merge BGRA, do not delete Phase 4 commits, do not progress past discussion until approval.

The mental model going forward: Phase 5 is the pixel pipeline (chrome included), Phase 4 is a small text-overlay step against per-variant slot coordinates, and the seam between them is a versioned scene PNG fixture. The compositor goes from a multi-module canvas pipeline to roughly one file (`render-text-overlay.js` + a `VARIANT_SLOTS` constant block).
</context>

<next_action>
Operator reads `.planning/phases/04-compositor/04-CONTEXT-RESET.md`. If approved as-is, the next command is `/gsd:plan-phase 04.2`. If revisions needed, edit the CONTEXT doc and re-discuss. Do NOT skip ahead.
</next_action>
