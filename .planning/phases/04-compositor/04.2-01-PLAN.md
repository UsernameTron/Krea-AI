---
phase: 04-compositor
plan: 04.2-01
type: execute
wave: 1
depends_on: []
files_modified:
  - mirror-post/src/brief/schema.js
  - mirror-post/src/brief/validator.js
  - mirror-post/test/fixtures/expected-briefs/brent-vellum.json
  - mirror-post/test/fixtures/expected-briefs/trevor-hustle.json
  - mirror-post/test/fixtures/expected-briefs/trevor-closer.json
  - mirror-post/test/fixtures/expected-briefs/pete-titles.json
  - mirror-post/CLAUDE.md
autonomous: true
requirements:
  - D-NEW-06
  - D-NEW-14

must_haves:
  truths:
    - "Post Brief v2 schema accepts scene_template as enum A|B|C|D"
    - "Post Brief v2 schema requires image_output.scene_png string field"
    - "image_seed override fields (environment, subject_pose, mood, color_temperature) are optional nullable in v2"
    - "All 4 fixtures validate under v2 schema"
    - "Validator rejects v1 briefs with a clear migration error mentioning schema_version"
    - "SCHEMA_VERSION exports '2.0'"
  artifacts:
    - path: "mirror-post/src/brief/schema.js"
      provides: "Post Brief v2 schema definition + validateBrief() accepting v2"
      contains: "SCHEMA_VERSION"
      exports: ["SCHEMA_VERSION", "SCHEMA", "validateBrief"]
    - path: "mirror-post/src/brief/validator.js"
      provides: "Semantic validator updated for v2 shape"
      exports: ["validateBriefSemantic"]
    - path: "mirror-post/test/fixtures/expected-briefs/brent-vellum.json"
      provides: "v2-migrated Brent Vellum fixture"
      contains: "schema_version"
    - path: "mirror-post/CLAUDE.md"
      provides: "Breaking-change note for v1->v2 migration"
  key_links:
    - from: "mirror-post/src/brief/schema.js"
      to: "mirror-post/src/brief/validator.js"
      via: "import { validateBrief } from './schema.js'"
      pattern: "validateBrief"
    - from: "mirror-post/test/fixtures/expected-briefs/*.json"
      to: "mirror-post/src/brief/schema.js"
      via: "schema_version field must be '2.0'"
      pattern: "schema_version.*2\\.0"
---

<objective>
Bump Post Brief schema from v1 to v2, migrate all 4 fixtures, and update the validator to accept v2 while rejecting v1 with a clear migration error.

Purpose: Phase 04.2 compositor rewrite (D-NEW-01/02/04) requires Post Brief v2 with `image_output.scene_png` path and `scene_template` enum A|B|C|D. The schema change is motivated by Phase 04.2 decisions, executed as a sub-plan per D-NEW-14.

Output: Updated schema.js, validator.js, 4 migrated fixtures, CLAUDE.md breaking-change note.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compositor/04-CONTEXT.md
@mirror-post/src/brief/schema.js
@mirror-post/src/brief/validator.js
@mirror-post/test/fixtures/expected-briefs/brent-vellum.json

<interfaces>
<!-- Current schema.js exports consumed by this plan -->
From mirror-post/src/brief/schema.js:
```javascript
export const POST_BRIEF_VERSION = "post-brief-v1";
export const SCHEMA = { version: POST_BRIEF_VERSION, ... };
export function validateBrief(brief) { ... }
```

From mirror-post/src/brief/validator.js:
```javascript
import { validateBrief } from './schema.js';
export function validateBriefSemantic(brief) { ... }
```
</interfaces>

<req_drift>
## REQ Drift Notes

This plan surfaces the following REQ-level issue for downstream amendment:

1. **REQ-X-060 conflict:** REQUIREMENTS.md states "3:2 horizontal" (line 195) but CONTEXT.md, operator brief, and all 04.2 decisions specify "16:9 1920x1080." The authoritative value is 1920x1080 (16:9) per CONTEXT.md D-NEW-09 and the operator's explicit sizing. REQUIREMENTS.md REQ-X-060 should be amended to read "16:9 horizontal (1920x1080)" in a future maintenance pass. Plans 04.2-01 through 04.2-05 use 1920x1080 throughout.

2. **REQ-X-052a/052b not yet in REQUIREMENTS.md:** CONTEXT.md D-NEW-04 defines the split; REQUIREMENTS.md still has only REQ-X-052. All 04.2 plans cite "REQ-X-052b (per CONTEXT.md D-NEW-04 split, pending REQUIREMENTS.md amendment)" for traceability.

3. **REQ-M-041/042 semantic remap:** REQ-M-041 ("text overlay renderer") maps to 04.2-04. REQ-M-042 ("tweet embed card renderer") is satisfied by non-rendering under the reset architecture -- tweet card is either baked into the chrome PNG or absent. No code renders it programmatically. Noted in 04.2-04.
</req_drift>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump schema.js to v2 with image_output and scene_template enum</name>
  <files>mirror-post/src/brief/schema.js</files>
  <behavior>
    - Test 1: SCHEMA_VERSION exports "2.0"
    - Test 2: validateBrief() accepts a valid v2 brief (with schema_version "2.0", image_output.scene_png string, scene_template enum A|B|C|D)
    - Test 3: validateBrief() rejects a v1 brief (no schema_version field or schema_version "1.0") with error containing "schema_version" and "migration" or "v2"
    - Test 4: image_seed.environment, subject_pose, mood, color_temperature are accepted as null (optional nullable overrides per D-NEW-06 C1)
    - Test 5: scene_template rejects values outside A|B|C|D enum
    - Test 6: image_output.scene_png is required and must be a string
  </behavior>
  <action>
    Modify mirror-post/src/brief/schema.js:

    1. Replace `POST_BRIEF_VERSION` with `SCHEMA_VERSION = "2.0"`. Update the SCHEMA object's version field.

    2. Add `"schema_version"` as a required top-level key (alongside meta, character, voice, post, props, tweet_embed, engagement, image_seed). Add `"image_output"` as a required top-level key.

    3. In `validateBrief()`, add an early gate: if `brief.schema_version` is missing or !== "2.0", return an error: `"Post Brief v1 detected (schema_version=${brief.schema_version ?? 'missing'}). Migrate to v2: add schema_version: '2.0', add image_output.scene_png, convert scene_template to A|B|C|D enum. See CLAUDE.md for migration guide."` This is a hard reject per D-NEW-06.

    4. Add validation for `image_output` block: `image_output` must be an object, `image_output.scene_png` must be a string.

    5. Modify `image_seed` validation: `scene_template` must be one of "A", "B", "C", "D" (enum check). Fields `environment`, `subject_pose`, `mood`, `color_temperature` change from required strings to optional nullable -- if present and not null, must be a string; if null or absent, accepted silently. Per D-NEW-06 C1, null means "use variant template default."

    6. Export `SCHEMA_VERSION` (not `POST_BRIEF_VERSION`) so downstream consumers use the new name. Keep validateBrief and SCHEMA exports.

    Write tests in mirror-post/test/schema-v2.js covering all 6 behaviors above before implementing.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Krea-AI/mirror-post && node test/schema-v2.js</automated>
  </verify>
  <done>SCHEMA_VERSION exports "2.0"; validateBrief accepts v2 shape; rejects v1 with migration error; scene_template is enum A|B|C|D; image_seed overrides are optional nullable; image_output.scene_png is required string.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 4 fixtures to v2 and update validator</name>
  <files>
    mirror-post/test/fixtures/expected-briefs/brent-vellum.json,
    mirror-post/test/fixtures/expected-briefs/trevor-hustle.json,
    mirror-post/test/fixtures/expected-briefs/trevor-closer.json,
    mirror-post/test/fixtures/expected-briefs/pete-titles.json,
    mirror-post/src/brief/validator.js,
    mirror-post/CLAUDE.md
  </files>
  <action>
    **Fixture migration (all 4 files):**

    For each of the 4 JSON fixtures in `mirror-post/test/fixtures/expected-briefs/`:

    1. Add `"schema_version": "2.0"` as the first top-level key.

    2. Add `"image_output": { "scene_png": "test/fixtures/scenes/variant-{x}.png" }` as a new top-level key (after engagement, before image_seed). The variant letter is derived from the existing `scene_template` value using D-NEW-05 mapping:
       - `"office-middle-mgmt"` (brent-vellum) -> `"B"` (executive strategy office / generic fallback)
       - `"airport-hustle"` (trevor-hustle) -> map to appropriate variant per D-NEW-05, likely `"C"` or `"B"` fallback
       - `"office-executive"` (trevor-closer) -> `"A"` or `"B"` fallback
       - Map pete-titles similarly. If unsure, use `"B"` (the D-NEW-05 fallback).
       Set scene_png path to `"test/fixtures/scenes/variant-b.png"` (or appropriate variant letter). These are placeholder paths -- actual PNGs come from Phase 5a.

    3. Convert `image_seed.scene_template` from the old string (e.g., `"office-middle-mgmt"`) to the new enum value (`"A"`, `"B"`, `"C"`, or `"D"`). Use the same variant mapping from step 2.

    4. Leave `image_seed.environment`, `subject_pose`, `mood`, `color_temperature` as-is (they remain as strings -- not null -- because the fixtures carry specific values that override the variant default).

    **Validator update:**

    In `mirror-post/src/brief/validator.js`, update `validateBriefSemantic()`:
    - The function already calls `validateBrief()` first, which now handles v2 schema validation (including the v1 reject gate). No additional schema checks needed in the semantic validator.
    - Verify the function still works with v2 shape (image_output present, scene_template is a single letter). No structural changes expected since the semantic validator only inspects text fields, safety, and voice -- none of which changed in v2.

    **CLAUDE.md breaking-change note:**

    Add a section to `mirror-post/CLAUDE.md` under "## Rules" or as a new "## Breaking Changes" section:
    ```
    ## Breaking Changes

    ### Post Brief v2 (Phase 04.2, 2026-04-15)
    - schema_version: "2.0" is now required (v1 briefs rejected with migration error)
    - image_output.scene_png: required string (path to Phase 5 scene PNG)
    - image_seed.scene_template: enum "A"|"B"|"C"|"D" (was freeform string)
    - image_seed.environment/subject_pose/mood/color_temperature: now optional nullable (null = use variant template default)
    - Motivated by D-NEW-06, executed per D-NEW-14
    ```
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Krea-AI/mirror-post && node test/schema-v2.js && node test/harness.js</automated>
  </verify>
  <done>All 4 fixtures have schema_version "2.0" and image_output.scene_png; scene_template is A|B|C|D enum; validator.js passes v2 briefs; harness.js passes; CLAUDE.md has breaking-change note.</done>
</task>

</tasks>

<verification>
1. `node test/schema-v2.js` -- all v2 schema tests pass (Task 1 test file)
2. `node test/harness.js` -- existing harness passes with v2 fixtures
3. `grep -c '"schema_version": "2.0"' mirror-post/test/fixtures/expected-briefs/*.json` -- returns 4 (all fixtures migrated)
4. `grep 'SCHEMA_VERSION' mirror-post/src/brief/schema.js` -- exports "2.0"
5. `node -e "import('./mirror-post/src/brief/schema.js').then(m => console.log(m.SCHEMA_VERSION))"` -- prints "2.0"
</verification>

<success_criteria>
- SCHEMA_VERSION constant exports "2.0"
- All 4 fixtures validate under v2 schema without errors
- v1 briefs (missing schema_version or schema_version "1.0") produce a clear migration error from validateBrief()
- image_output.scene_png is a required string in v2
- scene_template is constrained to enum A|B|C|D
- image_seed override fields accept null without error
- CLAUDE.md documents the breaking change
- Existing test harness continues to pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-compositor/04.2-01-SUMMARY.md`
</output>
