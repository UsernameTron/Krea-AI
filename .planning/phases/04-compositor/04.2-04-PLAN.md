---
phase: 04-compositor
plan: 04.2-04
type: execute
wave: 2
depends_on:
  - 04.2-01
  - 04.2-06
files_modified:
  - mirror-post/src/compositor/composite.js
  - mirror-post/src/compositor/index.js
  - mirror-post/package.json
requirements:
  - D-NEW-01
  - D-NEW-02
  - D-NEW-05
  - D-NEW-15
  - D-NEW-16
  - D-NEW-17
  - REQ-M-041
  - REQ-M-043
  - REQ-X-027
  - REQ-X-052b
  - REQ-X-060

must_haves:
  truths:
    - "compositePost() accepts a v2 Post Brief and returns a 1920x1080 PNG Buffer"
    - "Puppeteer renders linkedin-post.html with {{placeholder}} substitution to a PNG"
    - "Chromium version is pinned via exact puppeteer version in package.json (no caret, no tilde)"
    - "CSS @font-face references bundled Inter TTFs at src/compositor/fonts/, not system fonts"
    - "Scene PNG is selected deterministically via SHA-256(character.name + post.headline.text) mod pool_size"
    - "sharp.composite() layers use input: <PNG Buffer>, never raw: { channels: 4 }"
    - "Same brief produces byte-identical output (Buffer.compare === 0)"
---

<objective>
Implement the Path C compositor pipeline: render the shared LinkedIn post HTML template via Puppeteer (pinned Chromium) into a PNG, then composite a deterministically-selected FLUX-generated scene PNG into the template's `HERO_ZONE` rectangle via sharp.

Path C eliminates canvas text overlays, VARIANT_SLOTS coordinates, and per-variant full PNG assets. The compositor becomes: HTML template + placeholder substitution + Puppeteer render + sharp scene composite.

See `04-CONTEXT.md` D-NEW-16 (Puppeteer + pinned Chromium determinism) and D-NEW-17 (ONE shared HTML template).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-compositor/04-CONTEXT.md
@.planning/phases/04-compositor/04.2-01-SUMMARY.md
@.planning/phases/04-compositor/04.2-06-PLAN.md
@.planning/phases/04-compositor/04.2-07-PLAN.md
@mirror-post/src/compositor/composite.js
@mirror-post/src/compositor/templates/linkedin-post.html
@mirror-post/src/compositor/fonts/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Puppeteer with exact version in mirror-post/package.json</name>
  <files>mirror-post/package.json</files>
  <action>
    Add `puppeteer` to `dependencies` with an **exact** version pin (no `^`, no `~`). Record the bundled Chromium version in a comment field adjacent to the dependency line (e.g., in a top-level `"chromium_bundled_version"` note or inline README comment) so regeneration of golden fixtures can assert against it.

    Example (version numbers illustrative — executor picks the latest stable at install time and pins it):

    ```json
    {
      "dependencies": {
        "sharp": "0.34.5",
        "puppeteer": "23.11.1"
      },
      "_comment_chromium": "puppeteer 23.11.1 bundles Chromium 131.0.6778.204 — pinned for REQ-X-052b determinism"
    }
    ```

    Run `npm install` in `mirror-post/` after updating.
  </action>
  <done>
    puppeteer dependency present with exact version pin; node_modules populated; chromium version documented.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite compositePost() for Path C pipeline</name>
  <files>mirror-post/src/compositor/composite.js</files>
  <behavior>
    - Test 1: compositePost(v2Brief) returns a Buffer containing a 1920x1080 PNG
    - Test 2: Variant letter resolves from brief.image_seed.scene_template with fallback to B
    - Test 3: Scene index computed via SHA-256(character.name + post.headline.text) mod pool_size
    - Test 4: Same brief produces byte-identical output (Buffer.compare === 0) across two invocations
    - Test 5: All sharp.composite() layers use input: <PNG Buffer> (no raw: { channels: 4 })
    - Test 6: Clear error when scene-assets/{letter}/ is missing or empty
    - Test 7: Clear error when linkedin-post.html template is missing
    - Test 8: {{placeholder}} tokens in the rendered HTML are all substituted (no leftover {{ }} in the PNG's HTML source at render time)
  </behavior>
  <action>
    Replace `mirror-post/src/compositor/composite.js` with a Path C pipeline:

    **Pipeline steps (in order):**

    1. Read `brief.image_seed.scene_template` (A/B/C/D); fall back to `B` per D-NEW-05.
    2. Compute `hash = SHA-256(character.name + post.headline.text)` (hex).
    3. List files in `src/compositor/scene-assets/{letter}/` matching `scene-*.png`, sorted.
    4. `sub_n = parseInt(hash.slice(0, 8), 16) % files.length` — deterministic per D-NEW-15.
    5. Load the selected scene PNG as a Buffer via `readFileSync`.
    6. Load `src/compositor/templates/linkedin-post.html` as a string.
    7. Substitute all `{{placeholder}}` tokens with corresponding brief fields:
       - `{{profile_name}}` ← `brief.character.name`
       - `{{profile_title}}` ← `brief.character.title`
       - `{{post_label}}` ← static "Post" or brief-derived
       - `{{headline}}` ← `brief.post.headline.text`
       - `{{body}}` ← `brief.post.body.text`
       - `{{hashtags}}` ← `brief.post.hashtags.join(' ')`
       - `{{mini_profile_name}}` ← `brief.tweet_embed.author` (or handle)
       - `{{mini_handle}}` ← `brief.tweet_embed.handle`
       - `{{mini_body}}` ← `brief.tweet_embed.text`
       - `{{mini_hashtags}}` ← `brief.tweet_embed.hashtags.join(' ')`
       - `{{reaction_count}}` ← `brief.engagement.reactions.count`
       - `{{comment_count}}` ← `brief.engagement.comments`
       Escape HTML-unsafe characters in substituted values.
    8. Launch Puppeteer with the pinned bundled Chromium. Use:
       - `headless: 'new'`
       - `args: ['--no-sandbox', '--disable-dev-shm-usage', '--font-render-hinting=none']` (font-render-hinting=none helps cross-machine determinism but keep consistent)
       - Set viewport `{ width: 1920, height: 1080, deviceScaleFactor: 1 }`.
       - `page.setContent(substitutedHtml, { waitUntil: 'networkidle0' })`.
       - `page.evaluateHandle('document.fonts.ready')` to ensure @font-face loading completes before screenshot.
    9. Capture PNG screenshot via `page.screenshot({ type: 'png', omitBackground: false, clip: { x: 0, y: 0, width: 1920, height: 1080 } })`. Close browser.
    10. Load the template's exported `HERO_ZONE = { x, y, width, height }` constant (imported from a sibling module or parsed from the HTML file — executor picks simplest stable path; prefer a JS re-export at `src/compositor/templates/hero-zone.js`).
    11. Resize the scene PNG to `HERO_ZONE.{width,height}` via sharp (`fit: 'cover'`) and obtain a Buffer.
    12. Composite: load the Puppeteer screenshot Buffer with sharp, apply `.composite([{ input: sceneBuffer, top: HERO_ZONE.y, left: HERO_ZONE.x }])`, then `.png({ compressionLevel: 9, adaptiveFiltering: false })`, `.toBuffer()`.
    13. Return the final 1920×1080 PNG Buffer.

    **Critical constraints (enforce and document in comments):**

    - All `sharp.composite()` layers use `input: <PNG Buffer>` — **never** `raw: { channels: 4 }`. This is the BGRA-channel-swap mitigation from `tasks/lessons.md`. A pixel read at any chrome zone must match the authored HTML/CSS color within 2-unit RGB tolerance.
    - Chromium version is pinned via the exact-version `puppeteer` dependency (Task 1). Upgrading Puppeteer invalidates golden fixtures — this is intentional.
    - Fonts are bundled locally at `mirror-post/src/compositor/fonts/Inter-{Regular,SemiBold,Bold}.ttf` and referenced via `@font-face` relative URLs in the HTML template's `<style>`. Puppeteer must NOT fall back to system fonts — test by rendering with system Inter uninstalled.
    - Determinism hinges on this three-legged stool: pinned Chromium + bundled fonts + PNG-buffer composite. Violating any leg breaks REQ-X-052b.

    **Imports:** `sharp`, `puppeteer`, `node:fs`, `node:path`, `node:url`, `node:crypto`. Drop `renderTextOverlay` and `VARIANT_SLOTS` imports — both are dead under Path C.

    Write tests in `mirror-post/test/compositor-path-c.js`. Use the 4 reference fixtures to exercise all four variants and assert byte-identical repeat renders.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Krea-AI/mirror-post && node test/compositor-path-c.js</automated>
  </verify>
  <done>
    compositePost() implements the full Path C pipeline: HTML template → {{placeholder}} substitution → Puppeteer PNG render → sharp scene composite at HERO_ZONE → final 1920×1080 PNG. Byte-identical on repeat with identical inputs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update compositor barrel for Path C exports</name>
  <files>mirror-post/src/compositor/index.js</files>
  <action>
    Replace barrel contents with Path C surface:

    ```javascript
    export { compositePost } from './composite.js';
    export { HERO_ZONE } from './templates/hero-zone.js';
    ```

    Remove exports of `renderTextOverlay`, `buildStyledSegments`, `VARIANT_SLOTS` — all three are dead under Path C.
  </action>
  <done>Barrel exports only Path C surface. No references to deprecated text-overlay or VARIANT_SLOTS symbols.</done>
</task>

</tasks>

<verification>
1. `grep -E '"puppeteer":\s*"\^|"puppeteer":\s*"~' mirror-post/package.json` returns 0 (exact pin only)
2. `node test/compositor-path-c.js` — all tests pass, byte-identical assertion holds
3. `grep -c 'raw:' mirror-post/src/compositor/composite.js` returns 0 (no raw RGBA blocks)
4. `grep -c '{{' mirror-post/src/compositor/composite.js` — template substitution logic present, no leftover placeholders in rendered HTML
5. `grep 'system' mirror-post/src/compositor/templates/linkedin-post.html` returns 0 (no `font-family: system-ui` fallback — Inter only via @font-face)
</verification>

<success_criteria>
- compositePost(brief) returns a 1920×1080 PNG Buffer produced by Puppeteer + sharp
- Puppeteer uses a pinned-version Chromium via exact `puppeteer` version in package.json
- Bundled Inter TTFs referenced via `@font-face` in the template's CSS
- Scene selection deterministic via SHA-256(character.name + post.headline.text) mod pool_size
- Sharp composite uses PNG buffers exclusively — no `raw: { channels: 4 }` blocks
- Same brief produces byte-identical PNG output on repeat runs
- REQ-X-052b satisfied via the pinned-Chromium + bundled-fonts + PNG-buffer-composite stack
</success_criteria>

<output>
After completion, create `.planning/phases/04-compositor/04.2-04-SUMMARY.md`.
</output>
</content>
</invoke>