---
phase: 02-post-brief-generator
plan: 08
type: execute
wave: 5
depends_on: ["02-06", "02-07"]
files_modified:
  - mirror-post/test/integration.js
  - mirror-post/src/brief/index.js
autonomous: false
requirements:
  - REQ-M-010
  - REQ-M-011
  - REQ-M-012
  - REQ-M-013
  - REQ-M-014
  - REQ-M-015
  - REQ-M-016
  - REQ-M-017
  - REQ-M-018
must_haves:
  truths:
    - "End-to-end pipeline works: raw input -> classifyInput -> buildPrompt -> generateBrief -> validateBriefSemantic -> valid Post Brief"
    - "All 4 fixture input scenarios produce structurally valid Post Briefs"
    - "Freeform scenario produces original character name (not an archetype name from library)"
    - "Generated briefs pass both structural (schema.js) and semantic (validator.js) validation"
    - "Generation completes in under 15 seconds per brief"
  artifacts:
    - path: "mirror-post/test/integration.js"
      provides: "End-to-end integration test against 4 fixture inputs + 1 freeform"
    - path: "mirror-post/src/brief/index.js"
      provides: "Barrel export for brief module"
      exports: ["generateBrief", "classifyInput", "validateBrief", "validateBriefSemantic"]
  key_links:
    - from: "mirror-post/test/integration.js"
      to: "mirror-post/src/brief/generate.js"
      via: "generateBrief() call with real Anthropic API"
      pattern: "generateBrief"
    - from: "mirror-post/test/integration.js"
      to: "mirror-post/src/brief/validator.js"
      via: "validateBriefSemantic() on generated output"
      pattern: "validateBriefSemantic"
    - from: "mirror-post/src/brief/index.js"
      to: "mirror-post/src/brief/generate.js"
      via: "Re-export"
      pattern: "export.*from.*generate"
---

<objective>
Wire the complete Post Brief Generator pipeline end-to-end and run integration tests against all 4 fixture input scenarios plus 1 freeform scenario using real Anthropic API calls. Create the barrel export for the brief module.

Purpose: Validate that all Phase 2 modules work together: classifier -> prompt builder -> LLM generation -> semantic validator -> valid Post Brief. This is the final validation for all REQ-M-010 through REQ-M-018.
Output: `integration.js` test + `src/brief/index.js` barrel export.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-post-brief-generator/02-CONTEXT.md
@mirror-post/test/fixtures/expected-briefs/brent-vellum.json

<interfaces>
From mirror-post/src/brief/generate.js (Plan 06):
  - generateBrief(rawInput, options?) -> { brief, usage, model, fallback? }
  - BriefGenerationError (error class with code, message, details)

From mirror-post/src/brief/validator.js (Plan 07):
  - validateBriefSemantic(brief) -> { valid, errors, warnings }

From mirror-post/src/brief/input-classifier.js (Plan 03):
  - classifyInput(rawInput) -> { input_type, input_raw, archetype? }

From mirror-post/src/brief/schema.js (Phase 1):
  - validateBrief(brief) -> { valid, errors }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Barrel export for brief module</name>
  <files>mirror-post/src/brief/index.js</files>
  <read_first>
    - mirror-post/src/brief/generate.js (exports: generateBrief, BriefGenerationError)
    - mirror-post/src/brief/input-classifier.js (exports: classifyInput, getAllArchetypesMeta)
    - mirror-post/src/brief/validator.js (exports: validateBriefSemantic, SAFETY_ERROR_CODES)
    - mirror-post/src/brief/schema.js (exports: validateBrief, SCHEMA, POST_BRIEF_VERSION)
    - mirror-post/src/brief/structured-outputs-schema.js (exports: STRUCTURED_OUTPUTS_SCHEMA, OPTIONAL_PARAM_COUNT, DECISION_GATE)
    - mirror-post/src/brief/comedy-selector.js (exports: selectComedyStructures, getComedyStructuresMeta)
  </read_first>
  <action>
Create `mirror-post/src/brief/index.js` as the barrel export for the entire brief module:

```javascript
// Post Brief Generator — barrel export
// Pipeline: classifyInput -> buildPrompt -> generateBrief -> validateBriefSemantic

export { generateBrief, BriefGenerationError } from './generate.js';
export { classifyInput, getAllArchetypesMeta } from './input-classifier.js';
export { validateBrief, SCHEMA, POST_BRIEF_VERSION } from './schema.js';
export { validateBriefSemantic, SAFETY_ERROR_CODES } from './validator.js';
export { STRUCTURED_OUTPUTS_SCHEMA, OPTIONAL_PARAM_COUNT, DECISION_GATE } from './structured-outputs-schema.js';
export { selectComedyStructures, getComedyStructuresMeta } from './comedy-selector.js';
export { buildSystemPrompt, buildUserMessage } from './prompts/system-prompt-builder.js';
```
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Krea-AI/mirror-post && node -e "import { generateBrief, classifyInput, validateBrief, validateBriefSemantic } from './src/brief/index.js'; console.log('PASS: barrel exports resolve');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at mirror-post/src/brief/index.js
    - All listed exports resolve without error
    - generateBrief, classifyInput, validateBrief, validateBriefSemantic all importable from index.js
  </acceptance_criteria>
  <done>Barrel export wires all brief module components</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end integration test with real API calls</name>
  <files>mirror-post/test/integration.js</files>
  <read_first>
    - mirror-post/src/brief/index.js (just created — barrel exports)
    - mirror-post/test/fixtures/expected-briefs/brent-vellum.json (reference output to compare structure against)
    - mirror-post/test/harness.js (test pattern)
    - .planning/phases/02-post-brief-generator/02-CONTEXT.md (D-17: Structured Outputs primary, D-22: claude-opus-4-6)
  </read_first>
  <action>
Create `mirror-post/test/integration.js` (ESM) that runs REAL Anthropic API calls. Requires `ANTHROPIC_API_KEY` environment variable.

Test scenarios (5 total):

1. **Archetype exact match**: `generateBrief("resume-padder")`
   - Assert: brief passes validateBrief AND validateBriefSemantic
   - Assert: brief.meta.input_type === "archetype"
   - Assert: brief.character.name is a string with length > 0
   - Assert: brief.props.length >= 4
   - Assert: brief.post.body.text.length >= 100

2. **Archetype by name**: `generateBrief("The Demo Magician")`
   - Assert: brief passes both validators
   - Assert: brief.meta.input_type === "archetype"

3. **Domain match**: `generateBrief("contact-center")`
   - Assert: brief passes both validators
   - Assert: brief.meta.input_type === "domain"

4. **Freeform scenario (Brent Vellum input)**: `generateBrief("HR manager who strips soul from job descriptions")`
   - Assert: brief passes both validators
   - Assert: brief.meta.input_type === "freeform"
   - Assert: brief.character.name is NOT one of the 17 archetype names from library.json (REQ-M-018: original character)

5. **Freeform original scenario**: `generateBrief("Cloud architect who only deploys to PowerPoint")`
   - Assert: brief passes both validators
   - Assert: brief.meta.input_type === "freeform"
   - Assert: brief.character.name is NOT one of the 17 archetype names

For each test:
- Measure elapsed time, assert < 15000ms (REQ-M-017)
- Print: `PASS: {scenario} ({elapsed}ms, {input_tokens}+{output_tokens} tokens)`
- On failure: print `FAIL: {scenario}` with error details

At the end: print summary and total token usage across all tests.

If `ANTHROPIC_API_KEY` is not set, print `SKIP: integration tests require ANTHROPIC_API_KEY` and exit 0 (not exit 1 — don't break CI that lacks the key).

Add to package.json: `"test:integration": "node test/integration.js"`

Also add a combined test script: `"test:all": "node test/harness.js && node test/structured-outputs-audit.js && node test/distillate-version.js && node test/input-classifier.js && node test/comedy-selector.js && node test/system-prompt.js && node test/validator.js && node test/generate-mock.js"`

The integration test is intentionally NOT in test:all because it requires an API key and costs money.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Krea-AI/mirror-post && node test/integration.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists at mirror-post/test/integration.js
    - If ANTHROPIC_API_KEY is set: 5 test scenarios run and pass
    - If ANTHROPIC_API_KEY is not set: test prints SKIP and exits 0
    - Each generated brief passes both validateBrief and validateBriefSemantic
    - Freeform scenarios produce original character names (not from archetype library)
    - Each generation completes in under 15 seconds
    - package.json contains `"test:integration"` and `"test:all"` scripts
    - `npm run test:all` passes (all unit/mock tests, no API calls)
    - `npm test` (harness.js) still passes
  </acceptance_criteria>
  <done>End-to-end integration validated. All 4 fixture input types produce valid Post Briefs. Freeform scenarios generate original characters. Generation under 15 seconds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Post Brief quality and voice fidelity</name>
  <action>
    Verify the complete Post Brief Generator pipeline output:
    1. Run npm run test:all to confirm all unit/mock tests pass
    2. Run node test/integration.js to test 5 scenarios against real API
    3. Review generated Post Brief quality and voice fidelity
    4. Check freeform scenario produces original character
  </action>
  <what-built>Complete Post Brief Generator pipeline: input classifier, persona voice distillate, comedy selector, system prompt builder, LLM generation client, semantic validator, and integration tests. 8 plans across 4 waves.</what-built>
  <how-to-verify>
    1. Run `cd /Users/cpconnor/projects/Krea-AI/mirror-post && npm run test:all` — all unit/mock tests should pass (0 API calls)
    2. Run `cd /Users/cpconnor/projects/Krea-AI/mirror-post && node test/integration.js` — 5 scenarios against real API (requires ANTHROPIC_API_KEY)
    3. Review a generated Post Brief: does it feel like Mirror Universe Pete? Is the comedy sharp and specific? Are the props satirically relevant?
    4. Check the freeform scenario output: is the character truly original, not a copy of an archetype?
  </how-to-verify>
  <verify>User approves Post Brief quality</verify>
  <done>User confirms Post Brief Generator produces high-quality satirical content with correct voice</done>
  <resume-signal>Type "approved" or describe issues with generated Post Briefs</resume-signal>
</task>

</tasks>

<verification>
1. `cd mirror-post && npm run test:all` — all unit tests pass
2. `cd mirror-post && node test/integration.js` — 5 real API scenarios pass
3. Generated briefs pass both structural and semantic validation
4. Freeform scenarios produce original characters
5. Generation under 15 seconds per brief
6. User confirms Post Brief quality and voice fidelity
</verification>

<success_criteria>
- Complete pipeline works end-to-end: raw input -> classified -> prompted -> generated -> validated -> Post Brief
- All 4 fixture input types produce valid Post Briefs
- Freeform scenarios produce original characters (REQ-M-018)
- Generation under 15 seconds (REQ-M-017)
- System prompt under 8K tokens (REQ-M-016)
- All safety rails enforced by semantic validator (REQ-X-020-024)
- Pattern 2 Zero-Trust, Pattern 5 dual-position, Pattern 12 lazy loading all implemented
- User approves Post Brief quality
</success_criteria>

<output>
After completion, create `.planning/phases/02-post-brief-generator/02-08-SUMMARY.md`
</output>
