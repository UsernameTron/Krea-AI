---
phase: 02-post-brief-generator
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - mirror-post/src/brief/comedy-selector.js
  - mirror-post/test/comedy-selector.js
autonomous: true
requirements:
  - REQ-M-015
must_haves:
  truths:
    - "Comedy structures are loaded lazily — full definitions only when an archetype is selected"
    - "Selection returns 2-3 structures per archetype based on domain/category matching"
    - "All 13 structures (10 jokes + 3 roasts) are available for selection"
  artifacts:
    - path: "mirror-post/src/brief/comedy-selector.js"
      provides: "selectComedyStructures(archetype, count) function"
      exports: ["selectComedyStructures", "getComedyStructuresMeta"]
    - path: "mirror-post/test/comedy-selector.js"
      provides: "Test suite for comedy structure selection"
  key_links:
    - from: "mirror-post/src/brief/comedy-selector.js"
      to: "mirror-post/src/persona/comedy/structures.json"
      via: "JSON import, lazy full-definition loading"
      pattern: "import.*structures\\.json"
---

<objective>
Build the comedy structure selector that implements Pattern 12 lazy loading (REQ-X-032) for comedy structures. Metadata (id, name, description) is available upfront; full definitions (formula, examples) are loaded only when structures are selected for a specific archetype at generation time.

Purpose: REQ-M-015 (Pattern 12 lazy loading) applied to comedy structures. The system prompt builder (Plan 05) consumes 2-3 selected structures per D-08.
Output: `comedy-selector.js` module + test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-post-brief-generator/02-CONTEXT.md
@mirror-post/src/persona/comedy/structures.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Comedy structure selector with Pattern 12 lazy loading</name>
  <files>mirror-post/src/brief/comedy-selector.js, mirror-post/test/comedy-selector.js</files>
  <read_first>
    - mirror-post/src/persona/comedy/structures.json (10 joke_structures + 3 roast_structures, each with id, name, description, formula/example(s))
    - mirror-post/src/persona/archetypes/library.json (archetype categories: leadership, vendor, internal-blocker, contact-center, healthcare-rcm)
    - .planning/phases/02-post-brief-generator/02-CONTEXT.md (D-08: load all 13, select 2-3 per archetype; Pattern 12 lazy loading)
  </read_first>
  <behavior>
    - Test: getComedyStructuresMeta() returns 13 items, each with only {id, name, description} (no formula, no examples)
    - Test: selectComedyStructures({ category: "vendor" }) returns 2-3 structures with full definitions (formula + examples present)
    - Test: selectComedyStructures({ category: "leadership" }) returns structures including "resume-translation" or "credential-undercut" (domain-relevant)
    - Test: selectComedyStructures({ category: "vendor" }) returns structures including "vendor-bingo" or "demo-magician" related ones
    - Test: selectComedyStructures(null) returns 2-3 general-purpose structures (for freeform input)
    - Test: selectComedyStructures({ category: "leadership" }, 3) returns exactly 3 structures
    - Test: every returned structure has id, name, description, and either formula+examples or example (joke vs roast)
  </behavior>
  <action>
Create `mirror-post/src/brief/comedy-selector.js` (ESM) that imports comedy structures JSON.

**`getComedyStructuresMeta()`**: Returns all 13 structures with metadata only (id, name, description). Strips formula, examples, example fields. This is the Pattern 12 upfront-loading function.

**`selectComedyStructures(archetype, count = 3)`**: Returns `count` comedy structures with FULL definitions (including formula and examples).

Selection algorithm per D-08 and Claude's Discretion:
1. Build a domain-affinity map:
   - "leadership": ["credential-undercut", "resume-translation", "meeting-autopsy", "timeline-reality-check"]
   - "vendor": ["vendor-bingo", "technical-truth-bomb", "demo-magician" (not an id — use "witness-statement"), "timeline-reality-check"]
   - "internal-blocker": ["meeting-autopsy", "definition-corruption", "insider-callback"]
   - "contact-center": ["technical-truth-bomb", "escalating-specificity", "insider-callback"]
   - "healthcare-rcm": ["definition-corruption", "technical-truth-bomb", "insider-callback"]

2. If archetype is provided and archetype.category matches a domain key:
   - Select 1-2 structures from the domain-affinity list
   - Fill remaining slots with random structures NOT already selected (from full pool of 13)
   - Always include at least 1 roast structure (from the 3 roast_structures)

3. If archetype is null (freeform input):
   - Select from general-purpose structures: "escalating-specificity", "definition-corruption", "insider-callback"
   - Fill remaining with random

4. Return full structure objects (id, name, description, formula, examples for jokes; id, name, description, example for roasts)

Create `mirror-post/test/comedy-selector.js` following harness.js pattern. Add to package.json: `"test:comedy": "node test/comedy-selector.js"`
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Krea-AI/mirror-post && node test/comedy-selector.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists at mirror-post/src/brief/comedy-selector.js
    - `getComedyStructuresMeta()` returns 13 items without formula or examples fields
    - `selectComedyStructures({ category: "vendor" })` returns 2-3 items WITH formula/examples
    - `selectComedyStructures(null)` returns 2-3 general-purpose structures
    - `selectComedyStructures({ category: "leadership" }, 3)` returns exactly 3 items
    - At least 1 roast structure is included in every selection
    - package.json contains `"test:comedy"` script
    - Existing `npm test` still passes
  </acceptance_criteria>
  <done>Comedy selector implements Pattern 12 lazy loading. Metadata available upfront, full definitions loaded on selection. 2-3 domain-relevant structures selected per archetype.</done>
</task>

</tasks>

<verification>
1. `cd mirror-post && node test/comedy-selector.js` — all tests pass
2. `cd mirror-post && npm test` — existing tests pass
3. Metadata function returns 13 items with no full definitions
4. Selection function returns full definitions for domain-matched structures
</verification>

<success_criteria>
- Comedy selector correctly lazy-loads full definitions only on selection
- Domain-affinity matching produces relevant structures per archetype category
- Freeform fallback returns general-purpose structures
- Module is ready for consumption by system prompt builder (Plan 05)
</success_criteria>

<output>
After completion, create `.planning/phases/02-post-brief-generator/02-04-SUMMARY.md`
</output>
