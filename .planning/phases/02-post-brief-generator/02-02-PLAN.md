---
phase: 02-post-brief-generator
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - mirror-post/src/brief/prompts/persona-voice-distillate.md
  - mirror-post/test/distillate-version.js
autonomous: true
requirements:
  - REQ-M-011
  - REQ-M-012
must_haves:
  truths:
    - "Persona voice distillate exists as standalone file with version frontmatter"
    - "Distillate version matches mirror_pete.v2.json version field exactly"
    - "Distillate covers five axioms, banned phrases, deterministic transforms, and linguistic DNA"
    - "Version-assertion test catches version mismatch"
  artifacts:
    - path: "mirror-post/src/brief/prompts/persona-voice-distillate.md"
      provides: "900-word imperative prose distillation of persona spec voice sections"
    - path: "mirror-post/test/distillate-version.js"
      provides: "Version assertion test comparing distillate to spec"
  key_links:
    - from: "mirror-post/src/brief/prompts/persona-voice-distillate.md"
      to: "mirror-post/src/persona/spec/mirror_pete.v2.json"
      via: "version frontmatter must match spec version"
      pattern: "derived_from_spec_version"
---

<objective>
Create the persona-voice-distillate.md file: approximately 900 words of imperative prose distilled from mirror_pete.v2.json voice, philosophy, linguistic_dna, deterministic_transforms, banned_patterns, and safety sections. This is the artifact that gets injected into the PERSONA_VOICE slot of the system prompt (D-05). It is NOT the full persona spec but a generation-optimized compression.

Purpose: Separate the distillate lifecycle from the prompt builder. The distillate regenerates on spec version bumps (D-12) and has its own version-assertion test (D-10/D-11).
Output: persona-voice-distillate.md with frontmatter + prose, and distillate-version.js test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-post-brief-generator/02-CONTEXT.md
@mirror-post/src/persona/spec/mirror_pete.v2.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author persona-voice-distillate.md</name>
  <files>mirror-post/src/brief/prompts/persona-voice-distillate.md</files>
  <read_first>
    - mirror-post/src/persona/spec/mirror_pete.v2.json (FULL file. Read ALL sections: philosophy.axioms, voice.sliders, voice.texture, linguistic_dna, deterministic_transforms, banned_patterns, safety.hard_boundaries, emotional_register)
    - .planning/phases/02-post-brief-generator/02-CONTEXT.md (D-05 hybrid distillation, D-10/D-11/D-12 version sync)
  </read_first>
  <action>
Create `mirror-post/src/brief/prompts/persona-voice-distillate.md` with YAML frontmatter followed by imperative prose body.

Frontmatter fields (between triple-dash delimiters):
- derived_from_spec_version: "2.0.0"
- derived_from_file: "src/persona/spec/mirror_pete.v2.json"
- token_estimate: 900
- purpose: "PERSONA_VOICE slot in system prompt"
- regenerate_on: "persona spec version bump"

Prose body (approximately 900 words, imperative mood directed at the LLM):

The distillate MUST cover these sections from the spec, compressed into imperative generation instructions:

1. Voice Identity (from identity + philosophy.axioms): Who this voice is, the five axioms as generation rules. Cover: "Sarcasm Serves Insight" (every joke must contain a truth), "Specificity Is Credibility" (vague criticism is worthless), "Punch the System Not the Person" (roast patterns not individuals), "The Worldview Is Consistent" (pretension/hypocrisy/performative sincerity), "Earned Cynicism Not Performed" (experience-layer required).

2. Voice Calibration (from voice.sliders + voice.texture): Sarcasm 0.80, cynicism 0.85, warmth 0.25, satirical_intensity range 1-5. Texture rules: punchy short sentences mixed with complex ones, cool-to-cold tone, educated but not academic register, never start with "I", lead with observation, close paragraphs on sharp note.

3. Linguistic DNA (from linguistic_dna): Preferred words (precision_verbs, temporal, analytical, intensity, structural, dismissal). Sentence patterns: Setup-Payoff Pair, List Breaker, Redefinition, Quiet Data Point, Temporal Anchor. Paragraph rhythm: sharpest observation first, build with specificity, close with twist.

4. Deterministic Transforms (from deterministic_transforms): Include the full word_replacements table as a substitution list. Include cold_logic_prepends, sarcasm_parentheticals, and complexity_injections as insertion options.

5. Banned Patterns (from banned_patterns): Full list of ai_voice_markers and corporate_jargon that MUST NEVER appear in generated output. Frame as: "Never use these phrases in any generated content."

6. Safety Rails (from safety.hard_boundaries): All 5 hard boundaries as absolute rules. Frame as: "These are inviolable constraints on all generated content."

7. Warmth Windows (from emotional_register): When and how warmth appears. Include the contrast rule.

Writing style: Direct imperative instructions to the LLM. Not descriptive prose about the persona. Example: "Use punchy short sentences mixed with complex ones. The short sentence lands the blow." NOT "Pete tends to use punchy sentences."

Target: 850-950 words. This goes into an approximately 900 token system prompt slot.
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Krea-AI/mirror-post && node -e "import { readFile } from 'node:fs/promises'; const d = await readFile('src/brief/prompts/persona-voice-distillate.md', 'utf-8'); const fm = d.split('---')[1]; console.log('has_version:', fm.includes('derived_from_spec_version')); console.log('version_value:', fm.match(/derived_from_spec_version:\\s*\"(.+?)\"/)?.[1]); const words = d.split('---').slice(2).join('').trim().split(/\\s+/).length; console.log('word_count:', words); if (!fm.includes('2.0.0')) process.exit(1); if (words < 700 || words > 1100) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at mirror-post/src/brief/prompts/persona-voice-distillate.md
    - Frontmatter contains derived_from_spec_version with value "2.0.0"
    - Frontmatter contains derived_from_file referencing mirror_pete.v2.json
    - Body word count is between 700 and 1100
    - Body contains the word "sarcasm" (voice slider coverage)
    - Body contains "never" or "NEVER" (banned patterns coverage)
    - Body contains all 5 hard boundary themes including private individuals, punching down, harassment, minors, actionable harm
    - Body contains at least 5 entries from deterministic_transforms.word_replacements
    - Body is written in imperative mood (instructions to LLM, not descriptions about Pete)
  </acceptance_criteria>
  <done>Distillate file exists with correct version frontmatter and covers all required spec sections in approximately 900 words of imperative prose</done>
</task>

<task type="auto">
  <name>Task 2: Version-assertion test for distillate sync</name>
  <files>mirror-post/test/distillate-version.js</files>
  <read_first>
    - mirror-post/test/harness.js (existing test pattern)
    - mirror-post/src/brief/prompts/persona-voice-distillate.md (just created)
    - mirror-post/src/persona/spec/mirror_pete.v2.json (version field to compare against)
  </read_first>
  <action>
Create `mirror-post/test/distillate-version.js` (ESM, zero dependencies) that:

1. Reads src/persona/spec/mirror_pete.v2.json and extracts version field (currently "2.0.0")
2. Reads src/brief/prompts/persona-voice-distillate.md and extracts derived_from_spec_version from YAML frontmatter (parse between first two triple-dash delimiters, match derived_from_spec_version value)
3. Asserts they match exactly. If mismatch: FAIL message with both versions and exit 1
4. Also asserts the distillate file contains at least 5 of the banned ai_voice_markers from the spec (content-coverage sanity check)
5. Prints PASS with version info on success

Add to package.json scripts: "test:distillate": "node test/distillate-version.js"
  </action>
  <verify>
    <automated>cd /Users/cpconnor/projects/Krea-AI/mirror-post && node test/distillate-version.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists at mirror-post/test/distillate-version.js
    - Running node test/distillate-version.js outputs PASS and exits 0
    - Test would fail (exit 1) if distillate version were changed to "1.0.0"
    - package.json contains test:distillate script
    - Existing npm test still passes
  </acceptance_criteria>
  <done>Version assertion test passes, confirming distillate is in sync with persona spec v2.0.0. Per D-10/D-11, any spec version bump without distillate regeneration will be caught.</done>
</task>

</tasks>

<verification>
1. cd mirror-post and run node test/harness.js to confirm existing tests still pass
2. cd mirror-post and run node test/distillate-version.js to confirm version sync
3. Distillate word count between 700-1100
4. Distillate frontmatter declares version "2.0.0"
</verification>

<success_criteria>
- Persona voice distillate exists as standalone file with version-synced frontmatter
- Distillate covers all five axioms, voice calibration, linguistic DNA, deterministic transforms, banned patterns, safety rails, and warmth windows
- Version assertion test catches mismatches between distillate and spec
- Distillate is ready for consumption by the system prompt builder (Plan 05)
</success_criteria>

<output>
After completion, create `.planning/phases/02-post-brief-generator/02-02-SUMMARY.md`
</output>
