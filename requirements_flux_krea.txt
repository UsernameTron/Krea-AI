# Apple Silicon Optimized FLUX.1 Krea Requirements
# PyTorch with Metal Performance Shaders support
torch>=2.7.1
torchvision>=0.22.1
torchaudio>=2.7.1

# Core diffusers with MPS optimizations
diffusers>=0.34.0

# HuggingFace ecosystem
transformers>=4.54.1
huggingface-hub>=0.34.3
accelerate>=1.9.0

# Apple Silicon specific optimizations
# Install with: pip install --upgrade --force-reinstall torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Image processing with Apple optimizations
Pillow>=11.3.0

# Memory and compute optimizations
safetensors>=0.5.3
numpy>=2.2.0
tqdm

# Interactive use
click
jupyter

# Web UI with Metal backend support
gradio>=5.0.0

# System monitoring
psutil

# Apple MLX for Neural Engine acceleration (optional)
mlx>=0.19.0
mlx-lm>=0.19.0